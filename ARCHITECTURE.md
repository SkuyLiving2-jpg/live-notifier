# Architecture

Technical reference for the JKT48 IDN Live Discord notifier. This document assumes no prior context — it should be enough for a new engineer to understand the system, extend it safely, and know where to look when something breaks.

## 1. What this system does

The bot polls IDN Live's public GraphQL API every 30 seconds for livestreams by JKT48 members, and reacts to state changes:

- A member **starts** live → post a notification to a Discord channel (via webhook), optionally with an extra "flashy" DM to the bot owner if that member is on the personal priority list.
- A member **ends** live → post an "ended" notification, record the session's duration/peak-viewer stats.
- Along the way: viewer-count milestones, new personal-duration records, a daily recap, and an optional interactive chat-command bot (`cok ...`) that reads the same data.

It is a single always-on Node.js process (no database — everything is small JSON files), designed to run on Railway with a persistent Volume.

## 2. Directory map

```
scripts/
  cek-top-gifter.js         standalone manual CLI (not part of the 24/7 bot —
                           see §6)
  backup-data.js             pulls a full data backup from the deployed bot
  backfill-live-history.js   one-time: reconstructs pre-2026-09-19 live
                           sessions from Discord's own notification message
                           history, pushes them to the deployed bot (§9)
  set-channel-routing.js     pushes the local username->dedicated-channel-webhook
                           mapping (channel-routing.local.json, gitignored,
                           value optionally { webhookUrl, channelId }) to
                           the deployed bot — see §6's "per-member channels"
src/
  index.js                 entry point — require("./app").start() (this is
                           what "node src/index.js"/npm start actually runs)
  config.js                env loading + validation + all static config
                           (thresholds, priority member definitions, colors)
  app.js                   start(): wires everything together, the only
                           module (besides index.js/config.js) with real side effects
  security.js               HMAC request-signing helpers (used by src/server.js
                           and scripts/cek-top-gifter.js) — a leaf module like
                           config.js/utils.js, no dependency on the rest of src/
  discordClient.js         shared discord.js Client singleton
  idnApi.js                IDN Live GraphQL client
  server.js                HTTP server (health check + signed endpoints)
  monitor.js               the poll loop — orchestrates idnApi + storage + notify
  utils.js                 pure helpers: formatting, WIB time, text matching
  schedulePattern.js       computeSchedulePattern()/isHourInRange() — the
                           "most common live-time bucket" math shared by
                           chat/replies.js's "cok jadwal" AND
                           notify/priorityDm.js's heads-up alert (§10's
                           forty-third item); a standalone leaf module for
                           the same circular-require reason as
                           chat/interactionHelpers.js
  storage/
    jsonStore.js             generic cached JSON-file load/save factory
    activeLives.js            in-memory Map of who's live right now
    durationHistory.js        per-member rolling history of live durations (last 10, for averages)
    liveCount.js               per-member TOTAL live count since the bot started (never pruned)
    dailyLog.js                append-only archive of completed sessions (today/weekly/monthly recap)
    priorityStore.js           raw persistence for custom priority members
    subscriptions.js           per-user "ping me when X goes live" subscriptions
    gifterSnapshot.js          top-gifter snapshots (pushed from scripts/cek-top-gifter.js)
    channelRouting.js           username -> dedicated-channel-webhook mapping
                             (value optionally { webhookUrl, channelId } to also
                             enable §6's "Q3" fallback chat; pushed from
                             scripts/set-channel-routing.js) — §6
    headsUpAlerts.js           per-username "last alerted date (WIB)" dedup
                             for the heads-up heuristic below (§10's
                             forty-third item)
  priority/
    index.js                  priority-member domain logic (who's priority,
                             the flashy embed builder, the random thank-you
                             message generator)
  notify/
    webhook.js                shared "POST to the channel webhook" helper
    liveNotify.js              the plain start/end channel notification
    priorityDm.js               owner-only DMs: flashy notif, ending-soon
                             heuristic, Party Mode, "heads-up" schedule-
                             pattern heuristic (§10's forty-third item)
    publicAlerts.js             viewer milestones, new records, daily/weekly/
                             monthly auto-recap
    crashAlert.js                last-resort DM/webhook alert on an uncaught
                             error, sent right before the process exits
  chat/
    router.js                  buildChatReply() dispatcher + Discord event wiring
    replies.js                   pure reply-string builders + recap pagination
    menu.js                       fallback-menu UI + watch-confirm flow
    memberChannelReply.js          simplified 3-button fallback for a dedicated
                                 per-member channel (§6's "Q3")
    pendingState.js                short-lived per-user "waiting for a reply" state
    interactionHelpers.js          deleteInteractionMessage() - the one shared
                                 "Tutup" implementation every close button in
                                 the bot uses (§10's thirty-fifth item); a
                                 standalone leaf module specifically so
                                 menu.js/replies.js/memberChannelReply.js can
                                 all import it without a circular require
    compareFlow.js                 2-step dropdown/search UI for "cok bandingin"
                                 typed bare (no "<A> dan <B>") - button opens a
                                 search modal, matches become a select menu
                                 (or auto-picked if exactly one), same pattern
                                 for member B (excluding A), then the same
                                 comparison embed replies.js already builds
                                 for the typed form, with a "Tutup" button
                                 (§10's forty-fourth item)
    repeatedReplyGuard.js           when the exact same message text is typed
                                 more than twice in a row (same author, same
                                 channel), deletes the earlier typed messages
                                 AND the bot's earlier replies so only the
                                 newest pair remains (§10's forty-fifth/
                                 forty-sixth items)
data/                    JSON files (see §4) — local fallback; Railway uses a
                         mounted Volume instead (see §9)
tests/                   automated tests (see §10) — "npm test"
```

## 3. Data flow

```mermaid
flowchart TD
    A[monitor.js: pollLoop, every 30s] --> B[idnApi.fetchAllLivestreams]
    B --> C{diff against storage/activeLives Map}
    C -->|new username| D[notify/liveNotify.sendDiscordNotif start]
    C -->|username disappeared| E[notify/liveNotify.sendDiscordNotif end]
    C -->|still live| F[notify/publicAlerts: milestones + notify/priorityDm: ending-soon heuristic]
    D --> G[channel webhook: plain notif, every member]
    D --> H{is priority member?}
    H -->|yes| I[notify/priorityDm.sendPriorityDM: flashy DM to owner]
    H -->|yes, 2+ live together| J[notify/priorityDm.maybePartyModeAlert: DM]
    E --> G
    E --> K[storage/durationHistory + storage/dailyLog: record session]
    K --> L[notify/publicAlerts.maybeAnnounceNewRecord: channel]
    A --> M[notify/publicAlerts.maybeSendDailyRecap: once/day at DAILY_RECAP_HOUR]
    A --> N[notify/publicAlerts.maybeSendWeeklyRecap: once/week, Sunday at DAILY_RECAP_HOUR]
    A --> O[notify/publicAlerts.maybeSendMonthlyRecap: once/month, last day at DAILY_RECAP_HOUR]
```

The channel webhook (`DISCORD_WEBHOOK_URL`) is the only thing every server member sees. Everything under `notify/priorityDm.js` is a **DM to the owner only** — see §6.

## 4. Data model (`data/*.json`)

All files are cached in memory after first read (`storage/jsonStore.js`) and rewritten in full on every save — there's no database, so treat them as small enough to always fit in memory (they are, by design: duration history keeps only the last 10 entries per member, `daily-log.json` prunes anything older than 35 days).

**"Who's live right now" has exactly one source of truth: `active-lives-cache.json`.** `daily-log.json` only ever stores _completed_ sessions — it never duplicates the "is member X live" question. This wasn't always true: an earlier design tried to track both "open" and "closed" sessions in `daily-log.json` itself, which drifted out of sync with `active-lives-cache.json` twice in practice (see §10's bug notes) before being redesigned this way. Anywhere that needs "today's full picture" (ongoing + finished) merges the two at read time — see `chat/replies.js`'s `getTodaySessionsForRecap()`.

| File                         | Owner module                 | Shape                                                                                                                               | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------- | ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `active-lives-cache.json`    | `storage/activeLives.js`     | `{ [username]: { name, slug, liveAt, viewCount, peakViewCount, imageUrl, endingSoonAlerted, alertedMilestones[], missingStreak } }` | Who's live right now — survives restarts so the bot doesn't re-announce a live already in progress. The single source of truth for "is member X live." `missingStreak` (§10's eighteenth bug) counts consecutive poll cycles the member was absent from IDN's response — reset to `0` the moment they're seen again, only crossing `ENDED_GRACE_POLLS` (`monitor.js`) actually ends the session.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `live-duration-history.json` | `storage/durationHistory.js` | `{ [username]: [{ name, durationMs, at }] }` (max 10 entries)                                                                       | Last-10 completed lives per member. Powers `cok stats`, `cok kapan ... live`, and the ending-soon/new-record heuristics. `at` is the **end** timestamp, not start. Entries are kept sorted by `at` before slicing to 10, since `recordLiveDurationAt` (used by the backfill endpoint) can insert historical entries out of chronological order — `recordLiveDuration` (the normal live-tracking path) always appends the current moment, so this never mattered before backfilling existed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `live-count.json`            | `storage/liveCount.js`       | `{ [username]: { name, count, firstLiveAt, lastLiveAt } }`                                                                          | TOTAL completed-live count per member, never pruned/capped (unlike `live-duration-history.json`'s last-10). Powers `cok berapa kali <nama> live`. Incremented once per completed live, alongside `recordLiveDuration`/`recordLiveEnded` in `monitor.js`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `daily-log.json`             | `storage/dailyLog.js`        | `{ sessions: [{ name, username, startedAtUnix, endedAtUnix, durationMs, peakViewCount }], recapSentDate }`                          | Append-only archive of **completed** sessions only (pruned past 35 days) — written once, when a live ends, via `recordLiveEnded()`. `getCompletedSessionsToday()`/`getCompletedSessionsSince(daysBack)` filter by the WIB date (or a rolling window) of `endedAtUnix`. Powers `cok rekap`/`cok rekap minggu ini`/`cok rekap bulan ini`, `cok paling lama/rame` (today's variant merged with `activeLives` for the still-ongoing half; the weekly/monthly ones are closed historical windows, no merge). This archive only exists in this cross-day form since `16c4b7c` (2026-09-19) — before that, `dailyLog.js` reset `sessions` every WIB midnight, so nothing from before that rewrite can ever appear here. `getEarliestSessionDate()` exposes the archive's oldest record so `replyRecapRange()` can tell the user explicitly when a requested window (7/30 days) isn't fully covered yet, instead of silently showing a short recap that looks like a bug. |
| `custom-priority.json`       | `storage/priorityStore.js`   | `[{ rank, keyword, label, color, sirens }]`                                                                                         | Priority members added via `cok tambah prioritas <nama>`, beyond the 3 hardcoded ones (Nala/Levi/Lily).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `subscriptions.json`         | `storage/subscriptions.js`   | `{ [keyword]: [userId, ...] }`                                                                                                      | Per-user personal reminders (`cok ingetin <nama>`) — any member, any user, independent of the priority list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `gifter-snapshot.json`       | `storage/gifterSnapshot.js`  | `{ members: { [username]: { name, gifters, checkedAt } } }`                                                                         | Point-in-time top-gifter data, pushed from the local `cek-top-gifter` script (§6) — never fetched by the bot itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## 5. Chat command system (`src/chat/`)

`chat/router.js`'s `buildChatReply(text, {isBotChannel, channelId, authorId})` is the single entry point, called from `messageCreate`. Dispatch order matters:

1. **Pending-state shortcuts first** (checked before the wake-word gate, since none of these require saying "cok"): watch-confirm y/n (`chat/menu.js`), recap-page y/n (`chat/replies.js`), menu-shortcut digits (`chat/pendingState.js`), member-name-prompt follow-ups (`chat/pendingState.js`).
2. **Wake-word gate**: the message must contain "cok", OR be in the configured `BOT_CHANNEL_ID`, OR be in a channel mapped to a specific member via `channelRouting`'s `channelId` (§6's "Q3" — computed once near the top of `buildChatReply` and reused at step 4 too), OR look like a live-related question (contains "live" + a question word/`?`).
3. A long chain of regex/keyword matches against `replies.js` functions, most-specific first (e.g. subscribe commands before the generic priority-list check, since their text overlaps).
4. Falls through to either `chat/memberChannelReply.js`'s member-scoped 3-button fallback (in a dedicated channel) or `chat/menu.js`'s `replyFallbackMenu()` — a numbered menu (buttons + typeable digits) rather than silence.

### The 4 short-lived pending-state maps

All keyed `` `${channelId}:${authorId}` `` (never just `channelId`) with a TTL, so two people talking in the same channel never cross-wire each other's in-progress flow:

| Map                   | Lives in               | Set when                                           | Consumed by                                                                                                                                                                                                                                                            |
| --------------------- | ---------------------- | -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pendingWatchConfirm` | `chat/menu.js`         | User picks a live member (menu #4 or `"4 <nama>"`) | y/n (typed) → "gas nonton" or a status update; **or** the same choice via "🔴 Ya, nonton"/"Enggak"/"Tutup" buttons (§10's twenty-fifth item), which are self-contained and don't depend on this map to function — it's only consulted there as a display-name fallback |
| `pendingRecapPage`    | `chat/replies.js`      | A recap table has more pages                       | y/n → next page (also carries `rangeDays` — `null` for today, `7`/`30` for weekly/monthly, a `"YYYY-MM-DD"` string for a specific date (§10's twenty-seventh item) — so paginating a weekly recap doesn't silently switch to today's) or "oke, segitu aja"             |
| `pendingMenuByAuthor` | `chat/pendingState.js` | Fallback menu was just shown                       | Bare digit 1-9                                                                                                                                                                                                                                                         |
| `pendingMemberPrompt` | `chat/pendingState.js` | Menu #4/#9 picked with no name yet                 | Next message = the member name                                                                                                                                                                                                                                         |

(This replaced an earlier per-channel-only key, which caused a second person's identical digit reply in a shared channel to be misrouted back to the menu instead of resolved — see git history around commit `aca7035`.)

**Recap navigation also has a button path that bypasses `pendingRecapPage` entirely** (§10's twenty-first item): every recap table (`buildRecapPageBlock`) attaches a row of buttons — "Maju"/"Mundur" (whichever direction actually exists on that page), "Tutup rekap" (always), and "🔍 Cari member" (always) — whose `customId` is self-contained (`recap_nav:<next|prev>:<range>:<page>`, or the parameterless `recap_nav:close`/`recap_nav:search:<range>`), so a button on an old message stays clickable indefinitely with no TTL, unlike the typed "y"/"mundur" path above which still goes through `pendingRecapPage` and still expires after `PENDING_RECAP_PAGE_TTL_MS`. Both paths remain fully supported side by side and render through the same `buildRecapPageBlock()`, which also refreshes `pendingRecapPage` for that channel+author either way, so mixing typed and clicked navigation stays consistent — but they differ in delivery: the typed path still posts a new message (`message.reply()` in `chat/router.js`), while "next"/"prev"/"close" use `interaction.update()` to **edit the existing message the buttons are attached to** in place, rather than `interaction.reply()`, which would post a new one (§10's twenty-third item — the original button implementation used `reply()` too, spamming a new table message into the channel per click until the owner flagged it). "close" specifically updates the message to the fixed thank-you text with `components: []`, clearing the button row so the closed table can't be clicked again. "🔍 Cari member" opens a `ModalBuilder` text-input prompt (`recap_search_modal:<range>`) rather than a dropdown — chosen over a `StringSelectMenuBuilder` because the member list this feature is meant to scale to (~40+) exceeds Discord's 25-option select limit, where a genuine text search does not; its handler (`handleRecapSearchModalSubmit`) filters that range's sessions by given-name fragment and shows an un-paginated result table, deliberately simpler than the full recap flow since one member's sessions rarely span more than a page. That search reply also asks "Rekap sebelumnya masih mau ditampilin?" with "Ya, biarin"/"Enggak, hapus aja" buttons (§10's twenty-fourth item) — since the original table message is still sitting above the search result otherwise — whenever `interaction.message` was available on the modal submission (it's only populated when the modal was opened from a button on an existing message, which is always true for this flow's own "🔍 Cari member" button, but is defensively checked rather than assumed).

**"cok rekap" also has a per-date option (§10's twenty-seventh item)**: `getSessionsForRange(rangeDays)` is the one place that turns a `rangeDays` value into an actual session list — `null` → `getTodaySessionsForRecap()`, a `number` → `getCompletedSessionsSince(rangeDays)`, a `"YYYY-MM-DD"` string → `getCompletedSessionsForDate(rangeDays)` (`storage/dailyLog.js`) — introduced specifically so `tryHandleRecapPageShortcut`/`handleRecapNavButton`/`handleRecapSearchModalSubmit` couldn't each grow their own copy of this branch (they used to, duplicated three times) and silently miss the new date case. `encodeRecapRange`/`decodeRecapRange` gained a third form to carry a date through a `customId`'s colon-delimited segments without breaking the existing `split(":")` position-based parsing: a date is prefixed with `"d"` (`"d2026-09-13"`) rather than using its own `"-"` — never a `":"`, which would've split into extra segments and shifted every `parts[n]` after it. `buildRecapNavComponents` prepends an extra `ActionRowBuilder` (a `StringSelectMenuBuilder`, `customId: "recap_date_select"`, up to 25 options going back from today — see §10's thirty-second item for why it starts at today rather than yesterday, and why it's capped by whichever archive data actually exists rather than a flat 25) whenever `rangeDays` is a string, so a date-specific recap table always carries both the normal Maju/Mundur/Tutup/Cari row **and** a way to jump straight to a different date without closing and retyping "rekap tanggal" — reselecting the dropdown (`handleRecapDateSelect`) re-renders via `interaction.update()` on the same message, matching the in-place-edit convention every other recap button already uses, which is also literally what the owner asked for ("tabel tanggal sebelumnya di-delete biar pesannya gak berganda"). A date with zero sessions still gets the dropdown + a "Tutup rekap" button back (`buildCloseOnlyRow()`) rather than a bare unactionable string, so the user can immediately try another date. `chat/router.js`'s "cok rekap" dispatch chain gained two more specific checks (mirroring the existing "rekap minggu"/"rekap bulan" ones) before falling through to a genuinely bare "rekap": "rekap tanggal"/"rekap per tanggal" goes straight to the dropdown (`replyRecapDatePicker`), and "rekap hari ini"/"rekap hari" still goes straight to `replyTodayRecapSoFar` exactly as before (unchanged behavior, so an existing habit doesn't suddenly change). Only a truly bare "cok rekap" (none of minggu/bulan/tanggal/hari present) now shows `replyRecapMenu()` — four buttons ("Rekap hari ini"/"Rekap minggu ini"/"Rekap bulan ini"/"Rekap per tanggal", `customId: "recap_menu:<today|week|month|date>"`) instead of jumping straight to today's recap like it used to — `handleRecapMenuButton` edits that same menu message into whichever result was picked, reusing `replyTodayRecapSoFar`/`replyRecapRange`/`buildRecapDatePickerBlock` rather than duplicating any of their logic. Both `handleRecapMenuButton` and `handleRecapDateSelect` proactively clear `pendingRecapPage` for that channel+author **before** rendering their result — defensive, since these two handlers (unlike the older recap buttons) can repeatedly re-render substantially different tables into the same message across several clicks, and a result that happens to fit on one page never gets the chance to overwrite a stale multi-page `pendingRecapPage` left over from a previous choice the way `buildRecapPageBlock` normally would. Guarded by new `tests/replies.test.js` cases (dropdown option count/values/labels/`default` flag, the menu's four button `customId`s, each menu choice's `interaction.update()` behavior, a picked date with sessions vs. an empty one, the `"d<date>"` encoding surviving into the resulting table's own nav buttons, and the stale-`pendingRecapPage` clearing for both new handlers) and new `tests/router.test.js` cases for the "rekap tanggal" → picker and bare "rekap" → menu dispatch branches. "rekap hari ini" still routing directly (unchanged) is deliberately left untested at the router level, same as bare "cok rekap" used to be before this change — both dispatch to `replyTodayRecapSoFar`, which makes a real network call to the external archive (see this table's `tests/router.test.js` row).

**A twenty-eighth item, an owner-caught gap in the twenty-fifth item above**: the fallback menu's option 4/9 dropdown (`handleFallbackMenuButton`, picking which live member or gifter to check) never had a "Tutup" button of its own — only the _next_ step, the "mau nonton?" follow-up after a member was already picked, got one. Someone who opened the dropdown by mistake and didn't want to pick anyone at all had no way out except letting the (at the time, ephemeral) picker message sit there unanswered. Fixed with what was then `buildFallbackPickCloseRow()` (renamed `buildFallbackPickActionRow()` in the thirtieth item below, once it grew a second button), a second `ActionRowBuilder` (a `StringSelectMenuBuilder` must be alone in its own row, so the close button can't share the dropdown's row) appended under both the option-4 and option-9 dropdowns, `customId: "fallback_menu:close"` — deliberately routed through the _same_ `handleFallbackMenuButton` dispatcher every other `fallback_menu:` button already goes through (switching on `optionId`), rather than a new handler, since the action needs no member/gifter-specific data at all: edit the picker message itself to a fixed "Oke, dibatalin." with `components: []`. Guarded by new `tests/menu.test.js` cases: both dropdown replies now asserted to carry the close row as their second component, and the close button itself asserted to `update()` (never `reply()`) with the fixed dismiss text and no components. (The "ephemeral" detail here was superseded almost immediately after — see the twenty-ninth item below, which made the whole option-4/9 flow public.)

**A twenty-ninth item, the same day, once the owner noticed the pattern already fixed for recap tables hadn't been applied here**: every branch of `replyFallbackMenu()`'s 9-option menu (`handleFallbackMenuButton`/`handleFallbackMemberSelect`) still used `interaction.reply()` — clicking option 1, then option 3, then option 7 posted three separate new messages into the channel, exactly the pile-up the recap table's buttons (§10's twenty-third item) were fixed to avoid. The owner asked for the identical treatment here: clicking a different button edits the same message instead of spawning another one. Every `interaction.reply()` in both functions became `interaction.update()` — including, as a direct consequence, the option-4/9 member/gifter picker step, which had been sent `ephemeral: true` specifically so the "which member?" intermediate question wouldn't clutter the public channel (§10's twenty-eighth item, just above). There is no way to edit an already-public message into an ephemeral one (or vice versa) — a `MessageComponentInteraction`'s `.update()` always edits the exact message its button/select is attached to, keeping that message's original public/ephemeral-ness — and since `replyFallbackMenu()`'s own message is sent as a normal public `message.reply()` from `chat/router.js`, the picker step riding on the same message necessarily became public too. This is an accepted, deliberate trade-off (less private, but zero pile-up) rather than an oversight; nothing changed about who can click a given button, only about who can see the intermediate step. For the plain-string answers (options 1/2/3/5/6/7, plus the empty-state branches of 4/9), `buildFallbackMenuComponents()` is re-attached below the answer on every `update()`, so the menu stays clickable indefinitely from that one message — the user can keep bouncing between different options and it keeps re-editing the same place. Option 8 ("rekap hari ini") is the one exception: its reply already carries its own navigation row (Maju/Mundur/Tutup rekap/Cari member, §10's twenty-first/twenty-third items), so it's passed through `update()` as-is rather than layering the 9-option menu underneath it too — mixing two independent button systems on one message would confuse more than it'd help, and once someone drills into recap pagination from there, `handleRecapNavButton` fully owns that message's `components` from then on (its own "Tutup rekap" is the way out, not the top-level menu). Guarded by updated `tests/menu.test.js` cases across both functions: zero `interaction.reply()` calls anywhere in the chain, the re-attached menu after a plain-string answer, and the multi-step member-pick flow (option 4 → dropdown → pick → watch-confirm question) shown to compose entirely through successive `update()` calls on what is, start to finish, one single message.

**A thirtieth item, minutes after the twenty-ninth landed**: the option-4/9 picker's "Tutup" button (§10's twenty-eighth item) could only end the flow — there was no way back to the 9-option menu short of closing and re-triggering "cok bantuan" from scratch. The owner asked for a second button next to it: "Kembali," returning to the original menu. `buildFallbackPickCloseRow()` was renamed `buildFallbackPickActionRow()` and now returns both buttons in the one row under the dropdown — `customId: "fallback_menu:close"` (unchanged) and a new `customId: "fallback_menu:back"` — in that order, matching how the owner described them. `"back"` is handled in `handleFallbackMenuButton` right alongside `"close"`, since both are dispatched through the same `fallback_menu:` prefix already: it just calls `replyFallbackMenu()` again and `interaction.update()`s the picker message back into the original 9-button menu, continuing the twenty-ninth item's single-message pattern rather than opening a new one. Guarded by an updated `tests/menu.test.js` case asserting both buttons' `customId`/label in the action row, plus a new case for `"back"` asserting `interaction.update()` (never `reply()`) with `replyFallbackMenu()`'s exact content and all 9 option buttons present again.

**A thirty-first item, found during a general debugging pass, not an owner report**: `chat/memberChannelReply.js` (§6's "Q3") had been written _before_ the twenty-ninth item's edit-in-place fix existed and was never revisited once that pattern was established elsewhere — `handleMemberChannelFallbackButton` still used `interaction.reply()` for all seven of its actions, so clicking around in a per-member dedicated channel (today/count/status, then history_yes/watch_yes/history_no/watch_no) piled up a new message per click, the exact bug the twenty-ninth item fixed for the main 9-option menu, just left standing in this one file. There was also no "Tutup" button anywhere in this flow at all. Separately, `chat/menu.js`'s `handleFallbackMemberSelect` had two dead-end branches that returned a bare string with no components: the option-9 gifter-snapshot result, and option 4's "member already stopped live" not-found result — both left the message with zero buttons, unlike every other branch in the same flow, which forces retyping "cok bantuan" to get the menu back.

Both fixed the same way as the twenty-ninth/twenty-eighth items: every `handleMemberChannelFallbackButton` branch now goes through `interaction.update()`, `buildMemberChannelFallbackComponents()` grew a fourth "Tutup" button (still one row, under Discord's 5-per-row cap) handled via a new `"close"` action (`"Oke, dibatalin."`, `components: []`, matching `fallback_menu:close`'s wording), and a plain-answer branch (today; count/status when there's no data/not live; history_yes) re-attaches that same 4-button row so the message stays usable for follow-up questions — while a branch that already carries its own follow-up buttons (count/status with data, offering history/watch) is left as-is, and a terminal yes/no answer (watch_yes, history_no, watch_no) explicitly clears components, mirroring `handleWatchConfirmButton`'s "yes"/"no" terminal branches. `handleFallbackMemberSelect`'s two dead ends now re-attach `buildFallbackMenuComponents()` the same way every other branch of that function already did. Guarded by updated `tests/memberChannelReply.test.js` (full action dispatch asserted to call `update()` and never `reply()`, the reattach-vs-own-buttons-vs-clear distinction per action, the new "close" action) and `tests/menu.test.js`/`tests/router.test.js` cases (the two dead ends now asserting a 2-row menu is attached; the dedicated-channel fallback's button count updated from 3 to 4).

**A thirty-second item, an owner report the same day**: `buildRecapDateSelectRow` (§10's twenty-seventh item) had two related problems. First, its loop started at `i=1` (yesterday), deliberately skipping today — the comment reasoned "today already has its own 'Rekap hari ini' button in `replyRecapMenu`," but that's only true when the dropdown is reached by clicking through that 4-button menu; "cok rekap tanggal"/"cok rekap per tanggal" (`chat/router.js`) calls `buildRecapDatePickerBlock()` directly and never attaches that button at all, so today was simply never selectable from this dropdown through that path — reported as "tanggal hari ini gak ada." Second, the dropdown always offered 25 days back regardless of whether the bot had actually been tracking that far — since `daily-log.json` only has data from whenever the bot started (or was last redeployed with an empty Volume), picking an early date usually just landed on "belum ada live yang kecatet," a real but unhelpful option to be offering. Fixed together: the loop now starts at `i=0` (today included) and stops early once it would go earlier than `storage/dailyLog.js`'s `getEarliestSessionDate()` — the oldest date that's actually ever been recorded — so the dropdown only ever offers dates that could plausibly have data (still capped at 25 options either way, Discord's `StringSelectMenu` hard limit). Fixing the first half surfaced a second, smaller inconsistency: `getSessionsForRange(rangeDays)` (§10's twenty-seventh item) treated every string `rangeDays` as "look up completed sessions for this exact date" via `getCompletedSessionsForDate`, with no distinction for the string happening to equal today — harmless while today could never actually be selected as a string date, but now that it can, picking today from the per-date dropdown would silently exclude any session still in progress, unlike the dedicated "Rekap hari ini" button (`getTodaySessionsForRecap`, which merges completed sessions with `activeLives`). `getSessionsForRange` now special-cases a string equal to `getTodayWIB()` to route through `getTodaySessionsForRecap()` too, and `handleRecapDateSelect` was switched from calling `getCompletedSessionsForDate` directly to calling `getSessionsForRange(selectedDate)`, so both "today" entry points now behave identically. Guarded by new `tests/replies.test.js` cases, all run against an isolated empty/seeded daily-log (`freshRepliesForRecapRange`, not the file's shared accumulated state, so the assertions are deterministic): an empty archive still offers the full 25 options starting from today; an archive whose oldest session is 3 days old offers exactly 4 options (today through that oldest date) and none older; and picking today through `handleRecapDateSelect` with a currently-live `activeLives` entry (no completed sessions at all) still surfaces that member in the result.

**A thirty-third item, two owner reports at once**: first, `replyRecapRange` (weekly/monthly recap) never merged in `activeLives` — the original reasoning was that a 7/30-day window is "closed," so a member live _right now_ belongs to "today," not to the range, and would show up here on its own once it actually ended. That reasoning doesn't hold: a live in progress right now necessarily started within the last several hours, which is trivially within any 7- or 30-day window — so "cok rekap minggu ini"/"cok rekap bulan ini" was silently omitting whoever happened to be live at the moment it was asked. `getOngoingSessionsForRecap()` was extracted from `getTodaySessionsForRecap()` (same shape: `endedAtUnix`/`durationMs` both `null`) so it could be reused; `getSessionsForRange`'s numeric branch now merges it in (`getCompletedSessionsSince(rangeDays)` plus ongoing), which also means it flows automatically into pagination (`handleRecapNavButton`) and the search modal (`handleRecapSearchModalSubmit`) since both already go through `getSessionsForRange`. `replyRecapRange`'s summary numbers (total duration, "paling lama") are still computed from completed sessions only (`completed`, not the merged `sessions`) — an in-progress session's duration isn't final yet, same reasoning `replyTodayRecapSoFar` already applied by separating its own `completed`/`sessions`. The summary line also grew the same "(N udah selesai, M masih live)" breakdown `replyTodayRecapSoFar` already shows, and the "Paling lama" line is now conditionally omitted (`if (longest)`) for the edge case of a range with sessions but none of them completed yet.

Second, the owner asked for a way to jump directly to a specific page of a long weekly/monthly recap (page 5, the last page, etc.) rather than only stepping one page at a time via Maju/Mundur. `buildRecapNavComponents` now adds a "🔢 Lompat halaman" button, but only when `rangeDays` is a `number` (weekly/monthly specifically, as asked — "hari ini" and a specific date rarely run past a page or two) and `totalPages > 1` (no point offering it for a single-page table) — this keeps the button row at 5 buttons max (Maju + Mundur + Tutup + Cari + Lompat), Discord's per-row cap, exactly at the limit only on a page that has both Maju and Mundur available. Clicking it opens a `ModalBuilder` text prompt (`recap_jump_modal:<range>:<page>`, the same "Discord has no free-text button input" reason `"🔍 Cari member"` already uses a modal for) asking for a page number, or the words "awal"/"pertama"/"first" or "akhir"/"terakhir"/"last" for the first/last page — "last" doesn't need to know the actual page count in advance, since it just hands `buildRecapTablePage` a huge number (`Number.MAX_SAFE_INTEGER`) and lets its existing `Math.min(page, totalPages - 1)` clamp do the work, the same way an over-large typed page number harmlessly clamps to the last page rather than erroring. `handleRecapJumpModalSubmit` uses `interaction.update()` (edits the same message), unlike `handleRecapSearchModalSubmit`'s deliberate `interaction.reply()` (a separate message, so the original table stays visible to compare against) — a page jump is meant to _be_ navigation of that same table, not a side lookup. An input that doesn't parse as a number or either alias set doesn't error or revert to page 1 — it re-renders the _current_ page (carried in the modal's own `customId`, so no dependency on `pendingRecapPage`) with a short "gak ngerti" note appended, keeping the single-message in-place-edit convention intact even on bad input. Guarded by new `tests/replies.test.js` cases: `replyRecapRange` with only ongoing/no completed sessions (still shows, "Paling lama" line omitted), ongoing sessions appearing in the table but excluded from the "paling lama" comparison, the jump button's presence/absence across all four `rangeDays` shapes (via `buildRecapPageBlock` directly, no network dependency), the modal opening with the right `customId`, numeric/`"awal"`/`"akhir"` input landing on the right page, an out-of-range number clamping instead of erroring, and unparseable input falling back to the current page with the note - all via `update()`, never `reply()`.

**A thirty-fourth item, an owner idea while looking at the 9-option menu**: `replyFallbackMenu()`'s top-level menu never had its own close button at all — only the option-4/9 picker one step in (`fallback_menu:close`, §10's twenty-eighth item) did. The owner wanted a way to dismiss the menu itself for a plain misclick/mistype, and specifically asked for it to behave differently from the existing "close": not edited into a fixed "Oke, dibatalin." text (which still leaves one message sitting there as a trace), but the message deleted outright, as if the menu had never appeared. `buildFallbackMenuComponents()`'s second row (options 6-9, 4 buttons) had exactly one free slot under Discord's 5-per-row cap, so a "Tutup" button (`customId: "fallback_menu:delete"`, distinct from the picker's `"fallback_menu:close"` so the two don't collide or get confused with each other) was added there rather than needing a third row. `handleFallbackMenuButton`'s new `"delete"` branch calls `interaction.deferUpdate()` first — acknowledging the interaction with no visible response — before `interaction.message.delete()`; without the `deferUpdate()`, Discord shows the clicking user a "This interaction failed" error even though the deletion itself succeeds, since the interaction was never actually acknowledged. It also calls a new `clearMenuShown(channelId, authorId)` (`chat/pendingState.js`) to clear that pair's `pendingMenuByAuthor` entry, so a stray bare digit typed afterward within `PENDING_MENU_TTL_MS` doesn't get treated as "continuing" a menu that no longer visibly exists. `clearMenuShown` is `require("./pendingState")`'d _inside_ the branch rather than at the top of `menu.js` with everything else — `pendingState.js` already does `require("./menu")` at its own top level (for `resolveBareMenuChoice`/`startWatchConfirm`/`memberPromptQuestion`), so a top-level `require("./pendingState")` in `menu.js` would create a real circular require: whichever of the two loads first would hand the other a not-yet-populated `module.exports` at the moment of that early `require()`, silently capturing `undefined` for the destructured functions (CommonJS destructuring is a one-time snapshot, not a live binding, so this wouldn't self-correct once the module actually finished loading). A `require()` call written inside a function body instead only resolves the first time that branch actually executes — which, in a running bot, is always long after every module has already finished its initial load — so it reliably gets the fully-populated module without the timing hazard. Guarded by new `tests/menu.test.js` cases: `replyFallbackMenu()`'s second row now carries exactly `["fallback_menu:6", "fallback_menu:7", "fallback_menu:8", "fallback_menu:9", "fallback_menu:delete"]` (still one row, not a new third one) with the new button labeled "Tutup"; clicking it calls `deferUpdate()` once, calls `interaction.message.delete()` on that exact message, and calls neither `reply()` nor `update()`; and, separately, that a bare digit typed right after is caught by `tryHandleMenuShortcut` while the menu is still "open," but returns `null` (not caught at all) once the same channel+author pair's menu has been closed via this button.

**A thirty-fifth item, the next day, once the owner noticed the delete-vs-edit split from the item above**: the previous item made ONE "Tutup" button delete its message, but every other "Tutup"/"Tutup rekap" button in the bot (`fallback_menu:close` on the option-4/9 picker, `watch_confirm:close` on the "mau nonton?" question, `member_fallback:close` in a per-member dedicated channel, `recap_nav:close` on a recap table) still used the older edit-to-a-fixed-dismiss-text-plus-`components:[]` pattern — two different "close" behaviors coexisting was exactly the ambiguity the owner wanted gone: pick one and make every close button in the bot do it. Delete was kept (not edit) since it was the newer, more recently-requested behavior and leaves zero trace, which is what "just a misclick" calls for everywhere, not only on the 9-option menu.

Rather than copy the same `deferUpdate()` + `message.delete().catch(() => {})` pair into four separate files, it was extracted into a new leaf module, `chat/interactionHelpers.js`'s `deleteInteractionMessage(interaction)`. A **new, standalone module** was necessary rather than exporting it from whichever of `menu.js`/`replies.js`/`memberChannelReply.js` seemed most natural — `menu.js` already does `require("./replies")` at its own top, so if the shared helper lived in `replies.js` and `menu.js` imported it back, or if it lived in `menu.js` and `replies.js`/`memberChannelReply.js` imported it, either direction reproduces the exact circular-require hazard the previous item's `clearMenuShown` lazy-require was already written to avoid (see that item's entry for the full mechanics: a top-level circular `require()` hands one side of the cycle a not-yet-populated `module.exports`, silently capturing `undefined` for whatever was destructured at that moment, since CommonJS destructuring is a one-time snapshot rather than a live binding). `interactionHelpers.js` has no dependency on any of those three files and none of the three depend on each other for this helper, so it can be imported by all three with a normal top-level `require()` and no lazy workaround needed.

Four call sites converted, each keeping its own file-specific bookkeeping that has to run before the delete:

- `menu.js`'s `handleWatchConfirmButton`'s `"close"` branch — `pendingWatchConfirm` was already cleared unconditionally earlier in that function for every action, so no change needed there beyond swapping the reply body.
- `menu.js`'s `handleFallbackMenuButton`'s `"close"` and `"delete"` branches were **merged into one** (`if (optionId === "delete" || optionId === "close")`) — they're now byte-for-byte the same operation (`clearMenuShown` + `deleteInteractionMessage`), so keeping them as two near-identical blocks would just be duplicated code that could drift apart again later; the two distinct `customId`s (`fallback_menu:delete` on the 9-option menu, `fallback_menu:close` on the option-4/9 picker) stay as-is purely so a given click's origin is still distinguishable in logs/traces, with no behavioral difference intended. `fallback_menu:close` gained the `clearMenuShown` call it didn't have before, for the same reasoning the previous item applied to `fallback_menu:delete` — the picker is also the whole top-level menu flow ending, not a separate concern.
- `replies.js`'s `handleRecapNavButton`'s `"close"` branch — `pendingRecapPage.delete(...)` still runs first, unchanged; only the reply itself changed from an update-with-thank-you-text to a delete.
- `memberChannelReply.js`'s `handleMemberChannelFallbackButton`'s `"close"` branch — no extra state to clear here (this flow doesn't touch `pendingWatchConfirm`/`pendingRecapPage`/`pendingMenuByAuthor`), so this one became a one-line swap.

Guarded by updated tests across `tests/menu.test.js`, `tests/replies.test.js`, and `tests/memberChannelReply.test.js`: every one of the four close paths re-asserted to call `deferUpdate()` once, delete the interaction's own message, and call neither `reply()` nor `update()`; plus a new case confirming `fallback_menu:close` now also clears `pendingMenuByAuthor`, mirroring the existing `fallback_menu:delete` case from the item above. `tests/replies.test.js`'s shared `fakeInteraction` helper gained a `deferUpdate` spy and, whenever a test passes a `message` object, an auto-attached `.delete()` that records into the same `deletedMessageIds` array `channel.messages.delete` already used — tests that genuinely need `interaction.message` to be absent (the search-result defensive-fallback cases) are unaffected, since the default stays `undefined` unless a test explicitly supplies one.

**A thirty-sixth item, an owner report bundling several related gaps at once**: typing a fully-specific date straight into chat, e.g. "cok rekap 25 september," fell all the way through `chat/router.js`'s "rekap" dispatch chain to the bare `replyRecapMenu()` (four buttons) — none of the existing checks (minggu/bulan/tanggal/hari) recognized a literal day-plus-month in the sentence, so the user's already-explicit request was silently ignored in favor of asking them to pick again. The owner asked for this fixed, plus four related gaps in the same area: the 4-button recap menu had no "Tutup" of its own (every other recap surface already did); "cok rekap september" (a bare month name, no day) should show that month's recap directly, and once a future month's data exists, asking for a bulan that has data should surface it (not just September forever); "cok rekap senin"/"cok rekap hari senin" (and the other five weekdays) should offer a dropdown of which actual Monday, etc., to look at; and a bare "cok rekap bulan" (no month name, no "ini") should offer a dropdown across whichever calendar months actually have data, collapsing to that one month directly once there's only one (today's reality, since the bot only started 2026-09-13).

All new parsing lives in `chat/replies.js` as pure text → structured-data functions, kept separate from the data-fetching side so each is independently testable: `parseSpecificDateFromText` (day + Indonesian month name, either order, optional trailing year defaulting to the current WIB year) and `parseMonthOnlyFromText` (month name alone) both use the same `MONTH_NAMES_ID` alternation; `parseSpecificDateFromText` validates the day against `daysInMonth(year, monthIndex)` (built from `Date`'s own end-of-month rollover, so it's leap-year-correct without a manual table) and returns `null` — not a garbage date string — for something like "31 februari," specifically so a downstream `Intl.DateTimeFormat.format()` call never gets handed an `Invalid Date` and throws (the same class of bug already fixed once in `replySpecificMember`, see §10's earlier history). `parseWeekdayFromText` matches "senin".."sabtu" unambiguously anywhere in the text, but treats "minggu" (Indonesian for both "Sunday" and "week") specially — it only counts as the weekday Sunday when "hari" is _also_ present ("rekap hari minggu"), so the long-standing "rekap minggu ini"/"rekap minggu" (7-day rolling range) behavior can't be silently reinterpreted as a day-of-week request.

`chat/router.js`'s "rekap" branch was reordered from a flat list of independent `if`s into a single nested block, most-specific check first, so a sentence matching several keywords at once (e.g. "rekap per tanggal 25 september," which contains both "tanggal" and a parseable date) resolves to whatever the user most clearly meant: weekday name → specific full date → "minggu" (unchanged 7-day range) → "bulan"+"ini" (always the current calendar month, no dropdown, since the user was explicit) → bare month name → bare "bulan" → "tanggal" → bare "hari" → and only then the fully-bare `replyRecapMenu()`. "rekap bulan ini" changed meaning as part of this: it used to mean `replyRecapRange(30, ...)` (a rolling 30-day window), and now means the actual current calendar month (`replyRecapMonth`) — done deliberately so "cok rekap bulan ini" and "cok rekap september" give identical results while it's actually September, rather than two subtly different numbers for what a user would reasonably expect to be the same question. `recap_menu:month` (the 4-button menu's own "Rekap bulan ini") was updated to match.

Three new data-fetching functions in `chat/replies.js`, following the existing `getSessionsForRange`/`getOngoingSessionsForRecap` pattern: `getMonthSessionsForRecap(monthWIB)` merges `activeLives` in only when `monthWIB` is the calendar month actually in progress (a past month can never have anything "still live"), backed by two new `storage/dailyLog.js` exports (`getCompletedSessionsForMonth`, `getDistinctSessionMonths`, both simple filters/`Set` over the existing `sessions` array — no new storage shape). `getSessionsForRange` now tells a month string from a date string by length alone (`"YYYY-MM"` is always 7 characters, `"YYYY-MM-DD"` always 10 — fixed-width, so no ambiguity), and `encodeRecapRange`/`decodeRecapRange` grew a matching `"m"` customId prefix alongside the existing `"d"` one (§10's twenty-seventh item) for the same reason: keeping a month/date/day-count distinguishable after round-tripping through a colon-delimited `customId`. `buildRecapNavComponents` treats a month range exactly like a `number` range for the "🔢 Lompat halaman" button (§10's thirty-third item) — a full calendar month's table can run just as long as a 30-day rolling one — and attaches `buildRecapMonthSelectRow` instead of `buildRecapDateSelectRow` as its extra dropdown row, so switching months in place works the same way switching dates already did.

`replyRecapMonth(monthWIB, ...)` is the one function behind "cok rekap bulan ini," "cok rekap september," the 4-button menu's month choice, and the month dropdown's selection handler (`handleRecapMonthSelect`) — an empty result falls back to a month-picker dropdown (`buildRecapMonthSelectRow(getAvailableRecapMonths())`) when more than one month actually has data, or the plain 4-button `replyRecapMenu()` when there's only the one (empty) month to offer, mirroring the "don't offer a dropdown with one useless option" reasoning already used elsewhere. `replyRecapMonthGeneric()` (bare "cok rekap bulan") is a thin wrapper: `getAvailableRecapMonths()` (every distinct month with completed sessions, `Set`-deduplicated, plus the current month forced in even with zero completed sessions yet, since it's always a legitimate choice) collapses straight to `replyRecapMonth` when there's exactly one entry, or shows the picker otherwise. `replyRecapSpecificDate(dateWIB, ...)` is the direct answer to the original report — reuses `getSessionsForRange`/`buildRecapPageBlock` exactly like `handleRecapDateSelect` already does, and on zero sessions (before the bot's 2026-09-13 start, or just a genuinely quiet day) returns the "gak ada data" text immediately followed by the full 4-button `replyRecapMenu()`, per what the owner explicitly asked for. `replyRecapWeekdayPicker(weekdayIndex)` doesn't jump straight to a table — a weekday alone doesn't name one date — and instead offers a `buildWeekdayDateSelectRow`, the same dropdown machinery as `buildRecapDateSelectRow` but filtered to matching calendar dates (`findRecentDatesForWeekday`, bounded the same way — the archive's real oldest date via `getEarliestSessionDate`, or a fixed 90-day lookback cap when there's no data yet at all) and reusing the _same_ `recap_date_select` customId/handler, since picking one is functionally identical to picking any other date. Weekday-matching is computed via `WEEKDAY_FORMATTER_WIB.format(d)` (Intl, `timeZone: "Asia/Jakarta"`), never `Date.prototype.getDay()` — `getDay()` reads the system's _local_ timezone, and Railway's servers run UTC, so a WIB-midnight instant read back through `getDay()` would report the _previous_ day's weekday roughly a third of the time, the same class of timezone bug this codebase has consistently avoided elsewhere by always going through an explicit-timezone `Intl` formatter instead of a raw `Date` method.

Also answered, not just coded: the owner asked whether Discord chat bots can show an actual calendar-widget UI. They can't — Discord's component kit has buttons, select menus (string/user/role/channel/mentionable, capped at 25 options), and modal text inputs, with no native date-picker component — so the dropdown-of-real-dates approach already used for `buildRecapDateSelectRow`/now `buildWeekdayDateSelectRow` _is_ the practical "manual calendar," reusing the pattern rather than inventing a new one.

Guarded by new `tests/dailyLog.test.js` cases (`getCompletedSessionsForMonth`'s exact-month filtering and empty-month handling, `getDistinctSessionMonths`' de-duplication and empty-archive case), new `tests/replies.test.js` cases (all six parsers directly — valid/invalid/no-match inputs, including the `31 februari`/`32 september` invalid-day rejection and the "minggu"-needs-"hari" weekday special case; `buildRecapMonthSelectRow`'s labels/`default` flag; `replyRecapMonth`'s three branches — single-month-empty-fallback, multi-month-empty-dropdown, and populated-with-ongoing-merge-only-for-the-current-month; `replyRecapMonthGeneric`'s single-vs-multi-month collapse; `replyRecapSpecificDate`'s found/not-found cases; `replyRecapWeekdayPicker`'s found-dates and the genuinely-zero-matches case (an archive deliberately bounded to just today, asking for a different weekday); `handleRecapMonthSelect`'s update-not-reply behavior; and the "🔢 Lompat halaman" button appearing for a long month-range table), and new `tests/router.test.js` dispatch-order cases (a full date, a bare month name, bare "rekap bulan," a weekday name, "rekap hari minggu" landing on the weekday picker rather than the 7-day range, and a regression check that plain "rekap minggu" is unaffected).

**A thirty-seventh item, an owner report the next day**: `buildWeekdayDateSelectRow` (the previous item's weekday dropdown, "cok rekap senin" etc.) worked correctly for its own dropdown — but the moment a date was actually picked from it, the resulting recap table's own attached date-switcher dropdown (the extra row `buildRecapNavComponents` prepends whenever `rangeDays` is a date string, so a table can jump to a different date in place without closing and retyping) silently reverted to the full "every date" dropdown (`buildRecapDateSelectRow`) instead of staying filtered to that weekday — reported as "malah semua hari yang keluar, bukan tanggal-tanggal hari senin." Root cause: a picked date only ever flowed onward as a plain `"YYYY-MM-DD"` string — the same shape `getSessionsForRange`/`encodeRecapRange`/`buildRecapNavComponents` already treat any specific-date recap's `rangeDays` as — so nothing downstream of the initial pick carried any memory of "this came from a weekday-filtered dropdown, not the general one."

Fixed by tagging the _value itself_: `buildWeekdayDateSelectRow`'s options now encode `"YYYY-MM-DD#W"` (`encodeWeekdayTaggedDate`, `W` = 0-6) instead of a bare date, while `buildRecapDateSelectRow`'s (the general "rekap tanggal" dropdown) options stay untagged exactly as before — a plain-date pick should still be free to jump to any date, not get stuck on one weekday. Since every existing consumer of a date-shaped `rangeDays` already treats it as an opaque string (round-tripped through `encodeRecapRange`/`decodeRecapRange`'s `"d"` prefix, stored as-is in `pendingRecapPage`, passed through unmodified by Maju/Mundur/🔍 Cari member/🔢 Lompat halaman), the tag rides along for free through all of those — the only two places that needed to become tag-_aware_ were `getSessionsForRange` (strip the tag via a new `parseDateRangeValue` helper before querying `getCompletedSessionsForDate`, so session lookup keys off the real date) and `buildRecapNavComponents`/`handleRecapDateSelect` (check for a tag to decide whether to re-attach `buildWeekdayDateSelectRow(weekdayIndex, ...)` or the plain `buildRecapDateSelectRow(date)`, and to strip the tag before building a `Date`/label — `new Date("2026-09-14#1T00:00:00+07:00")` is an `Invalid Date`, so skipping this step would've thrown, the same class of bug already fixed once in `replySpecificMember` and guarded against in `parseSpecificDateFromText`). `handleRecapDateSelect`'s empty-result fallback (a picked date with zero sessions) got the same tag-awareness, so trying an empty Monday and getting "belum ada live" still leaves the Monday-filtered dropdown attached, not the full one.

Guarded by an updated `tests/replies.test.js` case (`buildWeekdayDateSelectRow`'s option values asserted to carry the `#W` tag, not a bare date) and two new cases reproducing the exact reported bug end-to-end through `handleRecapDateSelect`: picking a tagged date renders a table whose own date-select row still has the weekday-specific placeholder and every option still tagged to that same weekday (not the general dropdown), for both the populated-table branch and the zero-sessions fallback branch — plus a content-based check that the label never renders "Invalid Date."

**A thirty-eighth item, found during a general audit of `chat/menu.js` the owner asked for, not a report of an observed symptom**: `handleWatchConfirmButton`'s "member's no longer live" branch falls back to `pending?.name` for the display name when `activeLives` no longer has the entry (the member finished live between the button being shown and clicked). That fallback assumed `pendingWatchConfirm` — keyed only by `` `${channelId}:${authorId}` ``, not by which member or message it was for — still referred to the _same_ member as the button actually clicked. It doesn't necessarily: asking about a second member ("4 <nama lain>") before answering the first overwrites that same channel+author's `pendingWatchConfirm` entry, since `startWatchConfirmForEntry` always sets it unconditionally. If the _first_ member then goes offline before their (now-stale) message's button gets clicked, `entry` comes up empty and the fallback would hand back the _second_ member's name — a wrong-name response for whichever button was actually pressed, potentially naming someone who's still live as having "just finished." Fixed by only trusting `pending.name` when `pending.username` still matches the `username` carried in the clicked button's own `customId`; otherwise it now falls through to the existing generic `"member ini"` fallback rather than a specific-but-wrong name. Guarded by a new `tests/menu.test.js` case: two `startWatchConfirmForEntry` calls for different members under the same channel+author (simulating the second query overwriting the pending state before the first is answered), the first member removed from `activeLives`, then clicking the _first_ message's button — asserted to say `"member ini"`, never the second member's name.

## 6. Per-member notification routing (priority DMs + dedicated channels)

Nala/Levi/Lily (plus anything added via `cok tambah prioritas`) are the bot **owner's** personal favorites, not a server-wide setting. Since a webhook posts one identical message to everyone in the channel, there's no way to make the channel itself look different per viewer — so as of this session, the flashy treatment (embed, color, link button) moved entirely to DMs:

- **Channel** (`notify/liveNotify.js`): every member, priority or not, gets the same plain-content start/end notification — no title, no color, no buttons. A priority member can optionally get a couple of extra words woven into that same plain text via `startIntro`/`startHashtag` on their `PRIORITY_MEMBERS` entry (currently only Nala: an intro line before the message and `#NaLex` after — see `buildNormalPayload`'s `priority` param), but the message _structure_ never changes. `buildNormalPayload` also attaches the IDN thumbnail (`image_url`, same field the priority DM embed uses) as `embeds: [{ image: { url } }]` on a "start" notification when IDN provides one — no title/color/fields on that embed either, so it stays visually distinct from the priority DM's fuller embed while still being more eye-catching than plain text. Every notification also ends `content` with a `Mulai jam HH.MM.SS WIB` (start) or `Selesai jam HH.MM.SS WIB` (end) line (§10's twenty-second item) — the start time comes from IDN's own `live_at` (passed all the way from `monitor.js` through `sendDiscordNotif`'s `liveAt` param), not from when the bot happened to poll it, so it stays accurate even though polling only happens every 30 seconds; the end time is always "now" (the instant the bot detected the member as gone), since IDN doesn't report a real end timestamp. This is always appended as the LAST line specifically so `scripts/backfill-live-history.js`'s `START_RE`/`END_RE` (which only anchor the start of the string, not the end) keep matching old and new messages alike without needing an update. None of this touches the rest of `content`: `activeLives`/`daily-log.json` (what the recap reads) are recorded from the bot's own start/end bookkeeping, never parsed back out of the channel message text — so extra flavor text or an image embed is safe to add without risking a repeat of §10's format-change parsing bugs.
- **Owner DM** (`notify/priorityDm.js`, requires `DISCORD_BOT_TOKEN` + `PRIORITY_PING_USER_ID`): the flashy embed (colors, link button, Nala's randomized thank-you message), the "ending soon" duration heuristic, and "Party Mode" (2+ priority members live together) — all sent via `client.users.fetch(id).send(...)`, a private 1:1 DM channel nothing else in the server can read. Same start/end time is shown here too, as a dedicated `🕐 Mulai`/`🕐 Selesai` embed field (`buildPriorityPayload`'s `timestamp` option) — deliberately NOT folded into `embeds[0].description`, since `scripts/backfill-live-history.js`'s `parsePriorityEmbedEvent` reads that field verbatim as the member's name for "end" events.

### Per-member dedicated channels ("Q2")

Separate concern from priority members above — **any** member (not just Nala/Levi/Lily) can have their own dedicated channel (e.g. `#aralie-jkt48`), and this is orthogonal to priority status entirely. `sendDiscordNotif` (`notify/liveNotify.js`) sends the exact same start/end `payload` to **two** destinations when a mapping exists: the shared/default webhook (`DISCORD_WEBHOOK_URL`, unchanged from before) and, concurrently (`Promise.all`, not sequential — avoids doubling per-member latency in `monitor.js`'s per-member poll loop), the member's dedicated webhook from `storage/channelRouting.js`. This is **duplication**, not replacement — the shared channel keeps getting every member's notification regardless of whether they also have a dedicated one. Only the shared webhook's result determines `terkirim` (and therefore `activeLives` bookkeeping) — the dedicated send is best-effort, exactly like `sendPriorityDM`'s failure handling: a deleted/broken dedicated webhook logs an error but never blocks the shared notification or poll cycle. Scope is deliberately start/end live notifications only for now — milestone/record alerts and the daily recap stay shared-channel-only.

`storage/channelRouting.js` keys its map by the member's **exact IDN username** (e.g. `"jkt48_aralie"`), not a fuzzy keyword like `priorityStore.js`/`subscriptions.js` use — at the ~40+-member scale this feature is meant to scale to, an exact key sidesteps a whole-word-collision risk that was negligible at priority's 3-6-entry scale but becomes real at that size, and it's a simpler O(1) lookup besides. Keys are lowercased on save (`saveChannelRouting`) and the lookup also lowercases its input (`getChannelWebhookFor`) — same defensive posture as `idnApi.js`'s `isJkt48Member`, which already doesn't trust IDN's `creator.username` casing to be consistent. Found in practice: the owner's own local `channel-routing.local.json` had `"jkt48_Aralie"` (capitalized), which would have silently never matched the actual lowercase username IDN reports — no error, the dedicated-channel send would just never fire.

Managed via a **local file + signed push script**, not a chat command like `cok tambah prioritas` — deliberately, because a webhook URL is a bearer secret (whoever has it can post into that channel), and typing/pasting 40+ of them into Discord chat would leave them sitting in that channel's message history forever. `scripts/set-channel-routing.js` reads a local `channel-routing.local.json` (gitignored, same treatment as `.env`; `channel-routing.local.json.example` shows the shape) and signs+POSTs the whole map to `/api/channel-routing` (`server.js`'s `handleChannelRoutingUpload`, wired through the same `signedEndpoint()`/`requireSignedRequest` protection as `/api/backup` and `/api/gifter-snapshot`) — same `BOT_API_URL`/`API_SECRET` env vars already used by `cek-top-gifter.js`/`backup-data.js`. Every push is a **full replace**, never a merge: the pushed file is always treated as the complete, current list, so removing a member's channel is just deleting its line locally and re-running the script. Both the script and the server endpoint validate each value against Discord's webhook URL shape before accepting it (`discord.com` **or** the older-but-still-functional `discordapp.com` domain — a real webhook URL using the old domain was initially rejected by mistake, caught when the owner tried pushing one), so a typo/wrong-paste is caught immediately (400, listing which username's entry was bad) instead of silently failing on every future notification attempt for that member. `handleBackupExport`/`scripts/backup-data.js` include this mapping too, consistent with backup's "aggregate of every storage module" contract.

`scripts/backfill-live-history.js` intentionally stays scoped to the one shared channel only — dedicated channels are new, going forward, with no prior history to reconstruct.

If `DISCORD_BOT_TOKEN` is unset, none of this fires — the bot logs that at boot and priority members just get the plain channel notification like anyone else. If it's set but `PRIORITY_PING_USER_ID` is empty, the same degradation happens (logged separately in `chat/router.js`'s `clientReady` handler, specifically so this is checkable from Railway's Deploy Logs alone without needing dashboard access).

### Per-member channel fallback chat ("Q3")

Built on top of Q2, opt-in per member: a dedicated channel can also get a simplified 3-button fallback reply when someone chats in it and nothing else in `buildChatReply`'s dispatch chain matches. This required the routing entry to know the channel's own Discord ID, not just its webhook — so `channelRouting.js`'s value grew a second shape: either the original plain webhook-URL string (notification duplication only, no fallback chat — still fully supported, nothing breaks for members already set up this way) or `{ webhookUrl, channelId }` (notification duplication **and** the fallback chat below). `channelId` is filled in by hand from Discord's "Copy Channel ID" (Developer Mode), deliberately not resolved automatically from the webhook via an extra Discord API call — the owner already copies IDs by hand for `BOT_CHANNEL_ID`/`PRIORITY_PING_USER_ID` in `.env`, so this reuses a workflow that already exists instead of adding a runtime dependency and a cache-invalidation problem for something that changes rarely.

`storage/channelRouting.js`'s `getUsernameForChannel(channelId)` is the reverse lookup this enables — given an incoming message's `channel.id`, find which member (if any) that channel is dedicated to. It's a linear scan over the routing map (not a cached reverse index): at the ~40-entries scale this feature targets, that's cheap, and it avoids keeping a second data structure in sync every time the mapping is re-pushed. Only object-shaped entries with a `channelId` can match — a legacy plain-string entry never does, since the bot genuinely doesn't know which channel it corresponds to.

`chat/router.js`'s `buildChatReply` calls this lookup once near the top (not just at the fallback) and reuses the result twice: it's OR'd into `mentionsBot` alongside `isBotChannel`, so a dedicated channel bypasses the "cok"/live-question wake-word gate exactly like `BOT_CHANNEL_ID` does — almost any message in it is treated as addressed to the bot. (This was originally left gated the same as any ordinary channel; the owner reported the bot looking completely unresponsive in a dedicated channel, which turned out to be a plain message with no "cok"/live-sounding phrasing silently returning `null` at the wake-word check before ever reaching the dedicated-channel branch below.) Then, as the very last step of the dispatch chain — after every named command/pattern has already failed to match, exactly where it previously fell straight through to `menu.js`'s 9-option `replyFallbackMenu()` — the same lookup decides the fallback itself: if the channel is dedicated to a member, `chat/memberChannelReply.js`'s `replyMemberChannelFallback(username)` is returned instead, 4 buttons ("Udah live hari ini?", "Udah berapa kali live?", "Lagi live sekarang?", "Tutup", §10's thirty-first item) scoped to that one member, rather than the generic menu that would otherwise ask "which member?" (options 4/9) — a question this channel's whole existence already answers.

Each button resolves independently (`customId` shape `member_fallback:<action>:<username>`, dispatched by `handleMemberChannelFallbackButton`, always via `interaction.update()` — never `.reply()`, §10's thirty-first item): "today" and "status" answer directly from `activeLives`/`dailyLog.js`'s today-sessions; "status" additionally offers a Yes/No button pair ("mau nonton?") when the member is live right now, re-checking `activeLives` at click time (not trusting the state from when the button was first shown) before handing out the watch link — same staleness-safety pattern as `menu.js`'s existing `tryHandleWatchConfirmShortcut`. "count" reads the never-pruned total from `storage/liveCount.js` and, if there's any history at all, offers its own Yes/No pair ("mau liat history live-nya?") that on "yes" renders a table via `replies.js`'s `buildRecapTablePage` (first page only, no further pagination — deliberately simpler than "cok rekap"'s multi-page flow) filtered to that one member's sessions from `dailyLog.js` (so it can come up empty despite a nonzero total count, if all of that member's individual sessions have aged out of the 35-day retention window; the reply says so explicitly rather than looking broken). Both "no" branches ("nggak usah live history" / "nggak nonton") answer with the same fixed "terima kasih, enjoy" text.

Display name resolution (`resolveMemberDisplayName` in `memberChannelReply.js`) prefers `activeLives` (if currently live) then `storage/liveCount.js`'s last-known name, falling back to the raw IDN username if neither has ever been recorded for that member — this only shows up for a member added to the routing file who has never actually gone live since the bot started tracking them.

Validation of the new `{ webhookUrl, channelId }` shape lives in two places, same pattern as Q2's webhook-URL check: `scripts/set-channel-routing.js` (client-side, so a typo is caught on the owner's own machine) and `server.js`'s `handleChannelRoutingUpload` (server-side, the actual gate — `channelId` must match a Discord snowflake shape, `/^\d{17,20}$/`; this only rejects obviously-wrong values like an empty string or non-numeric text, it does not verify the channel actually exists, which would need a live Discord API call).

`scripts/cek-top-gifter.js` also needs `BOT_API_URL` + `API_SECRET` (see §9) but is a **separate, manually-run** local tool — it is never part of the always-on bot process, since fetching top-gifter data requires a personal IDN login that must never live on a 24/7 server.

## 7. Extension points

- **New chat command**: add a `reply*` function to `chat/replies.js`, then a matcher in `chat/router.js`'s `buildChatReply` chain (order matters — put more specific patterns before broader ones). Add a line to `replyHelp()`.
- **New priority member**: no code change needed — `cok tambah prioritas <nama>` (owner only). To change the 3 hardcoded ones' behavior, edit `PRIORITY_MEMBERS` in `config.js`.
- **New storage domain**: add a file under `storage/`, call `createJsonStore(filePath, defaultValue, { errorLabel })` from `storage/jsonStore.js`, wrap with domain-specific functions. Always pair a `load()` with a `save()` after mutating — the cache assumes nothing mutates without saving.
- **New notification type**: decide first whether it belongs in the shared channel (`notify/liveNotify.js` / `notify/publicAlerts.js`, visible to everyone) or the owner's DM (`notify/priorityDm.js`, visible to nobody else) — see §6 for why that distinction matters.

## 8. Environment variables

| Variable                          | Required            | Default                                   | Effect                                                                                                                                                                                                                                                                                                   |
| --------------------------------- | ------------------- | ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DISCORD_WEBHOOK_URL`             | **yes**             | —                                         | Bot exits at boot if missing. Channel notifications.                                                                                                                                                                                                                                                     |
| `POLL_INTERVAL_SECONDS`           | no                  | `20`                                      | How often (seconds) `monitor.js` polls IDN for live changes. Lower = faster detection, more IDN API load.                                                                                                                                                                                                |
| `DISCORD_BOT_TOKEN`               | no                  | —                                         | Enables chat commands + priority owner DMs.                                                                                                                                                                                                                                                              |
| `PRIORITY_PING_USER_ID`           | no                  | —                                         | Discord user ID that gets priority DMs and owns admin commands (`tambah/hapus prioritas`).                                                                                                                                                                                                               |
| `API_SECRET`                      | no                  | —                                         | HMAC secret for `/api/status`, `/api/gifter-snapshot`, `/api/backup`, and `/api/channel-routing`; those endpoints 503 without it.                                                                                                                                                                        |
| `CACHE_DIR`                       | no                  | `RAILWAY_VOLUME_MOUNT_PATH`, else `data/` | Where all JSON files live.                                                                                                                                                                                                                                                                               |
| `RAILWAY_VOLUME_MOUNT_PATH`       | no (set by Railway) | —                                         | Persists data across redeploys when a Volume is attached.                                                                                                                                                                                                                                                |
| `BOT_CHANNEL_ID`                  | no                  | —                                         | Channel where the bot replies without needing "cok"/"live".                                                                                                                                                                                                                                              |
| `ENDING_SOON_THRESHOLD_MINUTES`   | no                  | `40`                                      | Fallback threshold (minutes) for the ending-soon DM heuristic when a member has no duration history yet.                                                                                                                                                                                                 |
| `DAILY_RECAP_HOUR`                | no                  | `23`                                      | WIB hour the automatic daily recap fires.                                                                                                                                                                                                                                                                |
| `PORT`                            | no                  | `3000`                                    | HTTP health-check server port.                                                                                                                                                                                                                                                                           |
| `BOT_API_URL`, `API_SECRET`       | no                  | —                                         | `scripts/cek-top-gifter.js` (push gifter snapshots), `scripts/backup-data.js` (pull a data backup), `scripts/backfill-live-history.js` (push reconstructed historical sessions), and `scripts/set-channel-routing.js` (push the per-member dedicated-channel mapping) — same two env vars, same bot URL. |
| `IDN_AUTH_TOKEN`, `IDN_X_API_KEY` | no                  | prompts interactively                     | `scripts/cek-top-gifter.js` only — personal IDN credentials, never touches the 24/7 bot.                                                                                                                                                                                                                 |

## 9. Deployment (Railway)

- `package.json`'s `main`/`scripts.start` point at `src/index.js` (a thin `require("./app").start()`), run via `npm start` — no separate `js/` entry point exists anymore, everything lives under `src/`.
- Attach a Railway **Volume** and Railway auto-populates `RAILWAY_VOLUME_MOUNT_PATH`; `config.js` picks it up automatically (no code change needed) so `data/*.json` survives redeploys instead of resetting.
- `config.js` logs which `CACHE_DIR` is actually active at boot — check Railway's Deploy Logs first if stats/history seem to have reset unexpectedly.
- The HTTP server (`src/server.js`) exists purely so Railway's health check sees an open port; the bot itself is a background poller, not a web service.
- **Graceful shutdown**: `src/app.js`'s `start()` registers `SIGTERM`/`SIGINT` handlers — on redeploy/restart, Railway sends `SIGTERM` and waits before force-killing. The handler stops `monitor.js`'s poll loop (`stopPolling()` — lets an in-flight cycle finish, just skips scheduling the next one), destroys the Discord client, and closes the HTTP server, with a 5-second fallback `process.exit(0)` in case something hangs. Look for `"SIGTERM diterima, matiin bot dengan rapi..."` in Deploy Logs right before a redeploy's old instance stops.
- **Crash alerts**: `start()` also registers `uncaughtException`/`unhandledRejection` handlers that call `notify/crashAlert.js`'s `sendCrashAlert()` (DM to the owner if `DISCORD_BOT_TOKEN`/`PRIORITY_PING_USER_ID` are set, else the public webhook channel) before exiting — a last-resort net for something that escapes `checkLiveMembers()`'s own per-cycle try/catch, so a real crash doesn't go silently unnoticed until you happen to check Deploy Logs.
- **Data backup**: a Railway Volume survives redeploys, but it's still the _only_ copy of the bot's data — there's no automatic off-Railway backup. `npm run backup-data` (`scripts/backup-data.js`, same manual-CLI category as `cek-top-gifter.js` — not part of the 24/7 bot) pulls everything from a new signed `GET /api/backup` endpoint (`src/server.js`'s `handleBackupExport` — read-only, aggregates every storage module's current `load()`) and writes it to a timestamped file under `backups/` (gitignored, same reasoning as `data/`). Run it manually whenever you want a snapshot; nothing calls it automatically.
- **Historical backfill**: local scripts can't write directly to a Volume mounted on Railway, so `scripts/backfill-live-history.js` follows the same push pattern as `cek-top-gifter.js` instead of touching files locally — it reads the bot's own notification-channel message history via `DISCORD_BOT_TOKEN` (the channel webhook messages are the only surviving record of live sessions from before `daily-log.json` became a real archive, see §10), **plus, if `PRIORITY_PING_USER_ID` is set, the bot's own DM thread with the owner** (priority-member "JANGAN SAMPE KETINGGALAN!" notifications moved there entirely after 2026-09-18, see §10's eighth bug entry — that history exists nowhere else), reconstructs start/end pairs locally from both sources combined, then `POST`s them to a signed `POST /api/backfill-live-history` (`src/server.js`'s `handleBackfillLiveHistory`) which does the actual writing on the Railway side. Defaults to a dry run (preview only, now also logging each reconstructed session's name/start/end/duration, not just a total count); `npm run backfill-live-history -- --apply` commits. Safe to re-run **as many times as needed** — each session is deduped individually against what's already in `daily-log.json` (same username, `endedAtUnix` within a small tolerance — see §10's twelfth bug for why this replaced an earlier date-cutoff approach that broke under repeated runs), so a repeat run can't double-insert a session it already recorded, but also won't wrongly reject a genuinely new session just because it falls in a period some other session already covered. The accepted sessions are written to **both** `daily-log.json` (via `recordLiveEnded`) **and** `live-duration-history.json` (via `recordLiveDurationAt`, with the session's real historical end time — see §10's fifth bug entry for why the first version of this endpoint missed the second file) — `live-count.json` alone is rebuilt from every valid session received, not just the newly-accepted ones, since it covers the whole timeline by design (see the code comment in `rebuildLiveCountFromSessions`).
- **Repairing already-corrupted history**: if `backfill-live-history.js --apply` was run before the ninth-bug fix (§10) existed, the bad implausible-duration sessions it wrote are still sitting in Railway's Volume — fixing the reconstruction logic only stops _new_ corruption, it doesn't retroactively clean what's already there. `npm run repair-live-history -- --apply` (`scripts/repair-live-history.js`, same manual-CLI/dry-run-by-default pattern, needs only `BOT_API_URL`/`API_SECRET`) calls a signed `POST /api/repair-live-history` (`handleRepairLiveHistory`) that strips any `daily-log.json`/`live-duration-history.json` entry whose duration exceeds `MAX_PLAUSIBLE_LIVE_DURATION_MS`, then rebuilds `live-count.json` from what's left. Safe to run more than once — a second run reports 0 removed once the corrupted entries are gone.

### Troubleshooting: dashboard stuck showing "Building"

Railway's deploy badge can keep showing **"Building"** even after the **Build Logs** tab clearly finished (look for a green checkmark on `exporting to docker image format` and `image push` near the bottom — if those are there, the image itself built fine). This is almost always one of two things, in order of likelihood:

1. **The dashboard just hasn't refreshed.** Build finished, deploy is already progressing (or done), the badge is stale. Reload the page.
2. **The container is crashing right after start**, which Railway can sometimes still render as a lingering "Building" state before it flips to "Failed" or starts crash-looping. To tell the difference from (1):
   - Open the **Deploy Logs** tab (next to Build Logs) — that's where the actual `node src/index.js` process's output goes, not Build Logs.
   - Look for the boot sequence: `CACHE_DIR aktif: ...`, then `Bot notifikasi IDN Live jalan...`, and (if `DISCORD_BOT_TOKEN` is set) `Bot tanya-jawab login sebagai ...`. If all of these show up, the bot is actually running fine and the dashboard badge was just stale (case 1).
   - If Deploy Logs instead show a stack trace / `Error: Cannot find module ...` / the process exiting immediately, that's a real crash — paste that log, not the Build Logs, for diagnosis.

Things already verified as **not** the cause of this (so don't re-check them from scratch next time this happens, unless the codebase changes again in a way that could reintroduce them):

- Case-sensitivity of every `require()` path vs. the actual on-disk filename (Windows-dev/Linux-prod mismatches silently pass locally but crash on Railway) — audited with a small Node script that walks every `.js` file, resolves each relative `require()`, and compares it case-sensitively against `fs.readdirSync()` output.
- `package.json`'s `main`/`scripts.start` pointing at a real, existing file (`src/index.js`).
- No `Dockerfile`/`nixpacks.toml`/`railway.json`/`Procfile` overriding the start command — `package.json` is the only place it's declared.

## 10. Testing

`npm test` runs `node --test` (Node's built-in test runner — zero new dependencies, matches the project's existing "no framework unless it earns its weight" approach). Test files live under `tests/*.test.js`.

**CI**: `.github/workflows/test.yml` runs `npm test` on every push and pull request. **This gives visibility, not a deploy gate** — this repo's workflow is direct pushes to `master` with no branch protection or PR step, and Railway's deploy trigger isn't gated on GitHub Actions status by default. A failing test shows up as a red X on the commit on GitHub, but it does **not** by itself stop that commit from being deployed to Railway. Making it an actual gate would mean adding branch protection + a PR-based workflow, or a Railway-side check — neither is set up, since it'd change how this project is worked on day to day.

**Lint & format**: `npm run lint` (ESLint, flat config in `eslint.config.js`) catches real bugs (unused vars, undefined references, etc.) — `no-console` is deliberately off, since `console.log`/`console.error` to Railway's log stream is this codebase's entire logging strategy, not a leftover debug statement. `npm run format`/`format:check` (Prettier, `.prettierrc`) handles pure style, configured to match the codebase's existing conventions (double quotes, semicolons, a wide 150-char `printWidth` so the many long explanatory comments don't get hard-wrapped). Neither is wired into CI yet — they're available as local tools, run manually before a commit.

**Coverage is deliberately targeted, not exhaustive** — it covers the pure logic and storage behavior that's already proven fragile in practice (things this project has actually gotten wrong before), not every function in the codebase:

| File                                | What it covers                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `tests/utils.test.js`               | Pure formatting/text/WIB-time helpers (including `formatLongDateWIB`'s "13 September 2026" shape, §10's twenty-seventh item), plus `safeReplyOptions` always attaching `allowedMentions: { parse: [] }` regardless of whether the reply it wraps is a string or an object (§10's sixteenth bug).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `tests/jsonStore.test.js`           | The generic cache/load/save factory: default fallback, corrupt-file recovery, mkdir-on-save, persistence across a fresh instance (simulating a restart), and that a failed disk write (mocked `fs.writeFileSync`) does NOT desync the in-memory cache from what's actually on disk (§10's thirteenth bug).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tests/dailyLog.test.js`            | `recordLiveEnded` appending completed sessions correctly, `getCompletedSessionsForDate`'s exact-date filtering (§10's twenty-seventh item; `getCompletedSessionsToday` is now just this called with `getTodayWIB()`, covered by the same cross-midnight case), `getCompletedSessionsSince`'s rolling-window filtering, `getCompletedSessionsForMonth`'s exact-month filtering and empty-month case, `getDistinctSessionMonths`' de-duplication and empty-archive case (§10's thirty-sixth item), `getEarliestSessionDate`, the legacy-shape migration filter, and the 35-day retention pruning.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tests/liveCount.test.js`           | `recordLiveCompleted`'s per-username counting (increments correctly, `firstLiveAt` never changes after the first record, `lastLiveAt`/`name` update on later ones, members don't cross-contaminate each other's counts), `findLiveCountByNameFragment`'s fuzzy lookup, and `getLiveCountLeaderboard`'s count-descending ordering + `limit` truncation + empty-state; plus (§10's forty-fourth item) `searchLiveCountByNameFragment` returning every match sorted by name (not just the first), the empty/no-match cases returning `[]`, and a lowercase/"Kimmy JKT48"-style query still matching by first name.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tests/channelRouting.test.js`      | `loadChannelRouting`/`saveChannelRouting`'s full-replace-not-merge semantics, `getChannelWebhookFor`'s exact-username lookup — including that a username which merely starts with/contains a mapped one (e.g. `jkt48_intannia` vs. a mapped `jkt48_intan`) is deliberately NOT matched, unlike priority/subscriptions' fuzzy keyword matching, that both saving and looking up are case-insensitive, and that `getChannelWebhookFor` still resolves the object-shaped `{ webhookUrl, channelId }` entry correctly (§6); plus `getUsernameForChannel`'s reverse lookup (§6's "Q3") — an object entry's `channelId` resolves back to its username, an unmapped/empty/`null` channel ID returns `null`, and a legacy plain-string entry (no `channelId` at all) never matches even by its own webhook URL text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tests/priority.test.js`            | Custom priority add/remove validation, and `buildPriorityPayload`'s mention/end-message/start-intro-hashtag behavior, plus its `🕐 Mulai`/`🕐 Selesai` time field (always present regardless of `endMessagePool`, built from the `timestamp` option, defaults to "now" when omitted, and never leaks into `embeds[0].description` — see §6).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tests/liveNotify.test.js`          | `buildNormalPayload` stays plain-content (no title/color/fields) for every status/member, that a priority member's `startIntro`/`startHashtag` (Nala) get woven into that same plain text while a member without those fields (Levi/Lily/anyone else) is untouched, that the IDN thumbnail (`imageUrl`) becomes `embeds: [{ image }]` on a "start" notification when present, is absent when `imageUrl` is `null`, and is never attached on an "end" notification, and that `content` always ends with a `Mulai jam`/`Selesai jam` WIB-clock line built from the `timestamp` param (defaulting to "now" when omitted); plus `sendDiscordNotif` (real function, `global.fetch` mocked) handling a garbage/unparseable `liveAt` and a `null` `liveAt` without throwing, falling back to "now" for the `Mulai jam` line in both cases (§10's nineteenth bug); and the per-member dedicated-channel dual-send (§6) — a member with a `channelRouting` mapping gets exactly two webhook POSTs with identical payloads (default channel + dedicated one), a member without one gets exactly one (unchanged old behavior), and the dedicated-channel POST failing does NOT flip the overall result to `false` (mirrors `sendPriorityDM`'s existing non-fatal-failure invariant).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `tests/replies.test.js`             | `buildRecapTablePage` — sorting, pagination, and the cross-midnight "(DD/MM)" date marker — plus `getTodaySessionsForRecap`'s merge, `replyRecapRange`'s aggregation (including ongoing `activeLives` sessions appearing in the table but excluded from the total-duration/"paling lama" figures, and a range with only ongoing sessions and none completed yet omitting the "paling lama" line entirely — §10's thirty-third item) plus its "archive not fully covered yet" note, `tryHandleRecapPageShortcut`'s forward/backward page navigation (including rejecting "y" past the last page and "mundur" before the first, and that "n" still cancels outright — §10's eleventh bug), `replyLiveCount`, `replyLiveCountLeaderboard`'s count-descending ordering (relative-order assertions only, since this file accumulates `live-count.json` state across its own tests), `replyMemberStats`/`replySchedulePattern`'s bucket/weekday logic, `replyPriorityList`, subscribe/unsubscribe, the owner-gated priority add/remove, `replySpecificMember` rendering a normal `liveAt` correctly plus not throwing (and omitting the "mulai jam" clause) on a garbage or `null` `liveAt` (§10's nineteenth bug), `replyHelp` including the notification-settings tip (§10's twentieth item); and `handleRecapNavButton`/`handleRecapSearchModalSubmit`, the recap pagination buttons and member-search modal, including the in-place edit-vs-new-message behavior and the post-search keep/delete prompt (§10's twenty-first, twenty-third, and twenty-fourth items — see those entries for the detailed case lists); plus `buildRecapDateSelectRow`, `replyRecapMenu`/`replyRecapDatePicker`, and `handleRecapMenuButton`/`handleRecapDateSelect` (§10's twenty-seventh item — the per-date recap and "cok rekap" chooser menu), including today being offered/selectable and the dropdown being cut off at the archive's actual oldest date rather than always showing a flat 25 days, and today selected through the dropdown correctly merging in an ongoing `activeLives` session (§10's thirty-second item); plus the "🔢 Lompat halaman" button and `handleRecapJumpModalSubmit` — presence/absence across all four `rangeDays` shapes, the modal's `customId`, numeric/`"awal"`/`"akhir"` input, out-of-range clamping, and unparseable input falling back to the current page rather than erroring (§10's thirty-third item); plus `recap_nav:close` re-asserted (§10's thirty-fifth item) to call `deferUpdate()`, delete the interaction's own message, and call neither `reply()` nor `update()` — replacing the old assertion that it edited in a thank-you text; plus (§10's thirty-sixth item) `parseSpecificDateFromText`/`parseMonthOnlyFromText`/`parseWeekdayFromText` exercised directly (valid/invalid/no-match inputs, the invalid-calendar-day rejection, and "minggu"'s "hari"-gated special case), `buildRecapMonthSelectRow`'s labels/`default` flag, `replyRecapMonth`'s single-month-empty-fallback/multi-month-empty-dropdown/populated-with-current-month-only ongoing-merge branches, `replyRecapMonthGeneric`'s single-vs-multi-month collapse, `replyRecapSpecificDate`'s found/not-found cases, `replyRecapWeekdayPicker`'s found-dates case and the genuinely-zero-matches case, `handleRecapMonthSelect`'s update-not-reply behavior, and the "🔢 Lompat halaman" button also appearing for a long month-range table; plus (§10's thirty-seventh item) `buildWeekdayDateSelectRow`'s options asserted to carry the `"YYYY-MM-DD#W"` weekday tag rather than a bare date, and `handleRecapDateSelect` picking a tagged value re-asserted to keep the weekday-filtered dropdown attached (placeholder + every option's tag) on both the populated-table and zero-sessions-fallback branches, instead of silently reverting to the full "every date" dropdown; plus (§10's fortieth item) `resolveStatRangeFromText`'s four resolvable shapes (specific date, bare month name, bare "bulan", "minggu") plus its `null`/no-range case, and `replyLongestLiveForRange`/`replyTopViewersForRange` merging an in-progress session for the current month (with `replyLongestLiveForRange` computing a live duration from `startedAtUnix` rather than trusting the `null` placeholder) and their empty-range "belum ada data" messages; plus (§10's forty-first item) `buildExportCsv`'s header/rows for a mix of completed/still-live sessions and its comma-escaping, and `replyExportRecap`'s default/explicit-range/empty-range behavior including the empty case carrying no `files` at all. plus (§10's forty-seventh item) the recap member-search reply always carrying a "Tutup" button (`recap_nav:closesearch`) in every shape, surviving "Ya, biarin"/"Enggak, hapus aja", deleting the message when clicked, and leaving `pendingRecapPage` untouched. |
| `tests/router.test.js`              | `buildChatReply`'s dispatch chain — the wake-word gate, the explicitly order-sensitive regex pairs ("reminder" before "ingetin", "berhenti ingetin" before "ingetin", "rekap minggu/bulan/tanggal/hari" before plain "rekap" (§10's twenty-seventh item), "paling sering/paling banyak/tersering/terbanyak" + "live" before plain "siapa yang live"), one case per major command branch, both directions of `"berapa kali <nama> live"` (keyword-first and name-first, including the "cok" wake-word-not-swallowed-into-the-name regression), the fallback-menu path, and (§6's "Q3") that an unmatched message in a channel mapped via `channelRouting`'s `channelId` gets `memberChannelReply.js`'s 3-button per-member fallback instead of the generic 9-option menu, while an unmapped channel is unaffected and still gets the generic menu; plus "cok rekap tanggal" dispatching to the date-picker dropdown and a genuinely bare "cok rekap" dispatching to the 4-button chooser menu (§10's twenty-seventh item), both of which are safe to exercise for real since neither touches the network; plus (§10's thirty-sixth item) the reordered "rekap" dispatch chain's new specific checks — a full date ("25 september") to `replyRecapSpecificDate`, a bare month name ("september") to `replyRecapMonth`, bare "rekap bulan" to `replyRecapMonthGeneric`, a weekday name ("senin") to `replyRecapWeekdayPicker`, "rekap hari minggu" landing on the weekday picker rather than the 7-day range, and a regression check that plain "rekap minggu" (no "hari") is unaffected. Deliberately skips anything that dispatches to `replyTodayRecapSoFar` (bare "cok rekap" before this session, "cok rekap hari ini" now), since that makes a real network call to the external archive — see `tests/menu.test.js`'s note; `replyRecapRange` has no such network call, so the weekly/monthly recap dispatch tests exercise the real function; plus (§10's fortieth item) "paling lama live minggu ini"/"paling rame ditonton bulan ini" dispatching to the range-aware `replyLongestLiveForRange`/`replyTopViewersForRange` rather than the "hari ini" default, also network-free; plus (§10's forty-first item) "cok export rekap" dispatching to `replyExportRecap` (a file-carrying reply) rather than being swallowed by the general "rekap" dispatch chain; plus (§10's forty-fourth/forty-sixth items) the comparison dispatch: "bandingin"/"bandingkan"/"banding" with "dan" as the only separator, the bare "<A> dan <B>" form (wake-word gated, and not hijacking ordinary sentences), "vs" no longer accepted, same-member inputs refused, and a bare/one-name keyword dispatching to `replyStartComparePick()` rather than the generic fallback menu.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `tests/menu.test.js`                | `resolveBareMenuChoice`'s dispatch table, the watch-confirm y/n flow (including re-checking live status at confirm time, and a non-y/n answer not consuming the pending state), and the button/select-menu handlers via lightweight fake `interaction` objects (`{ customId, channelId, user: { id }, values, reply, update }` — the code never touches anything else on a real discord.js interaction, so no mocking library is needed). Asserts every captured `interaction.reply()` call arrives as `{content, ..., allowedMentions: { parse: [] }}` (§10's sixteenth bug), never a bare string; plus `handleWatchConfirmButton` (§10's twenty-fifth item) — "yes"/"no" editing the message in place (`interaction.update()`), "close" now deleting it outright (§10's thirty-fifth item), re-checking live status by the `customId`-carried username rather than trusting stale data, falling back to `pendingWatchConfirm`'s stored name when the member's already gone from `activeLives` **and** that stored entry's `username` still matches the button actually clicked (§10's thirty-eighth item — otherwise the generic "member ini" fallback, never a different member's name), and clearing that pending entry so a stray typed "y"/"n" afterward doesn't double-answer; plus `buildFallbackPickActionRow()` (§10's twenty-eighth/thirtieth items) — both the option-4 and option-9 dropdown replies asserted to carry the "Tutup"/"Kembali" row as their second component, and `fallback_menu:back` asserted to `update()` back to `replyFallbackMenu()`'s exact content with all 9 option buttons present again; plus (§10's twenty-ninth item) every branch of `handleFallbackMenuButton`/`handleFallbackMemberSelect` asserted to call `interaction.update()` and never `interaction.reply()`, and the plain-string answer branches asserted to carry `buildFallbackMenuComponents()` re-attached underneath; plus (§10's thirty-fourth/thirty-fifth items) both `"fallback_menu:delete"` (the top-level menu's own "Tutup", its `customId`/label in the menu's second row) and `"fallback_menu:close"` (the dropdown's "Tutup," §10's twenty-eighth item) — now merged into one branch in `handleFallbackMenuButton` — asserted to call `deferUpdate()` once, delete the interaction's own message, call neither `reply()` nor `update()`, and clear that channel+author's `pendingMenuByAuthor` entry (via `chat/pendingState.js`'s `markMenuShown`/`tryHandleMenuShortcut`, imported directly into this test file) so a bare digit typed afterward is no longer caught as a menu continuation.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `tests/monitor.test.js`             | `stopPolling()` is safe to call before any poll cycle has run, and idempotent. Deliberately doesn't exercise `pollLoop()` itself — it calls the real IDN API. Does exercise `checkLiveMembers()` directly, across multiple consecutive calls, with `global.fetch` mocked (no real network): the implausible-duration guard (§10's fifteenth bug) — IDN reports nobody live and a manually seeded stale `activeLives` entry is forced through the "just ended" path, confirming an implausible (>12h) duration is discarded from the daily log while a plausible one still gets recorded normally; the missing-poll grace period (§10's eighteenth bug) — a mock that simulates IDN's own paginated response shape across a queue of per-cycle "who's live" snapshots confirms a single miss is tolerated (session stays open under its original `liveAt`, `missingStreak` resets once seen again) while two consecutive misses still correctly end and record the session; and, end-to-end through `checkLiveMembers()` itself (captured outgoing webhook bodies, not just a direct `buildNormalPayload()` call), that the "start" notification's `Mulai jam` line reflects IDN's own `live_at` (not the poll time) and the "end" notification carries a `Selesai jam` line; and `computeNextPollDelay` (§10's twentieth item) — a fast cycle gets the full interval minus elapsed time, and a cycle that took as long as (or longer than) the interval itself still gets at least `MIN_POLL_DELAY_MS`, never zero/negative.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `tests/crashAlert.test.js`          | `sendCrashAlert()` never throws, for both an `Error` object and a bare string reason (the two shapes `uncaughtException`/`unhandledRejection` can hand it) — exercises the real webhook-fallback path against the dummy test webhook URL (a fast, deterministic rejection from Discord's real API, not a flaky third party) rather than mocking `fetch`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `tests/webhook.test.js`             | `withDefaultMentionGuard`'s default-vs-caller-override behavior, `postToWebhook`'s actual outgoing request body (via a mocked `global.fetch`, no real network) always carrying `allowed_mentions` — `{ parse: [] }` by default, or the caller's own value (e.g. `{ users: [...] }`) when set (§10's sixteenth bug) — and the 429 retry/backoff: retries and eventually succeeds after a single rate-limit response, gives up and returns `false` after `MAX_RATE_LIMIT_RETRIES` consecutive 429s, does NOT retry a non-429 failure (e.g. 500) at all, and still retries (using a 1-second fallback delay, `global.setTimeout` mocked so the test doesn't actually wait) when the 429 response body itself isn't valid JSON; plus the optional `webhookUrl` param (§6) posting to that explicit URL when given, and falling back to `DISCORD_WEBHOOK_URL` when omitted (backward compatible with every pre-existing call site).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `tests/server.test.js`              | The HTTP server, started for real on an OS-assigned local port (no external traffic — health check and `/api/*` are pure localhost) — signed vs. unsigned `/api/backup` requests, the backup response shape, that `/` stays unauthenticated, an oversized request body being rejected (413) before signature verification even runs (`security.js`'s `MAX_REQUEST_BODY_BYTES`), and `/api/backfill-live-history`'s dry-run/apply modes, invalid-session filtering, the implausible-duration server-side rejection (§10's ninth bug), the per-session dedup idempotency (calling it twice with the same sessions doesn't double-insert, a genuinely new session from an already-covered period is still accepted, and a near-duplicate a few seconds off from an existing session is still caught — §10's twelfth bug), and that accepted sessions also land in `live-duration-history.json` with their real historical `at` timestamp (not "now"); plus `/api/repair-live-history`'s dry-run/apply modes, that it strips implausible-duration entries from both `daily-log.json` and `live-duration-history.json`, and that dry-run genuinely changes nothing. The backfill/repair-writing tests use a fresh, isolated server/storage instance (`freshStartServerForBackfillTest`) so exact-count assertions don't depend on what earlier tests in the same file happened to write; plus `/api/channel-routing` (§6) — signed vs. unsigned, a full-replace push (a second push with different content fully supersedes the first, doesn't merge), a malformed webhook-URL value being rejected (400, naming the bad username) without touching whatever was already saved, that both `discord.com` and the older `discordapp.com` domain are accepted, and (§6's "Q3") that the object-shaped `{ webhookUrl, channelId }` value is accepted when `channelId` is a valid Discord snowflake and rejected (400, naming the bad username) when it isn't.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `tests/memberChannelReply.test.js`  | `replyMemberChannelFallback`'s 4-button initial reply (3 questions + "Tutup", §10's thirty-first item) and display-name resolution (live now / `liveCount`'s last-known name / raw username fallback, §6's "Q3"); each of the 3 question options' direct answers (`replyMemberLiveToday`'s today-count combining completed sessions + a currently-ongoing one, `replyMemberLiveCount`'s total-count text and its conditional Yes/No history buttons appearing only when there's any recorded count, `replyMemberLiveStatus`'s live/not-live branches and its conditional Yes/No watch buttons appearing only when live); `replyMemberLiveHistoryTable` rendering a filtered-to-one-member table when sessions exist versus its "no longer stored" text when they don't (retention-pruned or never tracked); `replyMemberWatchNow` re-checking `activeLives` at click time and handling the member having ended live in the meantime; and `handleMemberChannelFallbackButton`'s full action dispatch table via lightweight fake `interaction` objects (same pattern as `tests/menu.test.js`) — every action asserted to call `interaction.update()` and never `.reply()` (§10's thirty-first item), the reattach-4-button-menu-vs-keep-own-buttons-vs-clear-to-empty distinction per action, both "no" branches converging on fixed final text with `components: []`, every reply carrying `allowedMentions: { parse: [] }`, an unrecognized action calling neither `reply()` nor `update()`, and "close" (§10's thirty-fifth item) now asserted to `deferUpdate()` and delete the message rather than update it to dismiss text.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `tests/publicAlerts.test.js`        | `buildDailyRecapPayload` — the embed's title/color/timestamp, and its fields' values (total sessions/unique members, total duration, and picking the actually-longest session) computed correctly from a handful of hand-built sessions, including a member who went live twice the same day. `maybeSendDailyRecap` exercised for real (`DAILY_RECAP_HOUR` forced to `0` via an env var set before `config.js` is first required, so its hour gate always passes regardless of the real time the test runs at) with `global.fetch` mocked: sends exactly once when there's at least one completed session today, never sends (but still stamps `recapSentDate`, so it doesn't re-check every cycle) when there are none, and never sends a second time for the same day; plus (§10's thirty-ninth item) `buildWeeklyRecapPayload`/`buildMonthlyRecapPayload`'s embed shape, `isSundayWIB`/`isLastDayOfMonthWIB` as pure functions against fixed dates (including leap-year February), and `maybeSendWeeklyRecap`/`maybeSendMonthlyRecap` exercised with an injected `now` for both an open gate (sends once, dedups a second call the same day) and a closed one (sends nothing, and leaves `recapSentWeek`/`recapSentMonth` unset so the next real occurrence isn't silently skipped).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `tests/config.test.js`              | `envInt`'s env-var parsing (indirectly, through `DAILY_RECAP_HOUR`/`DEFAULT_ENDING_SOON_THRESHOLD_MS`) — an explicit `"0"` is honored as `0` rather than silently falling back to the default (§10's seventeenth bug), an unset or empty-string env var does fall back, and a non-numeric value falls back rather than producing `NaN`; plus `POLL_INTERVAL_MS` (§10's twentieth item) defaulting to 20 seconds and honoring `POLL_INTERVAL_SECONDS` when set. Re-requires `config.js` fresh (clearing `require.cache`) for each combination, restoring `process.env` afterward so it doesn't leak into other assertions in the same file.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `tests/backfillLiveHistory.test.js` | `scripts/backfill-live-history.js`'s pure logic (no real Discord/network calls) — `extractWebhookIds`, `parseEvents`'s message-format matching and webhook-identity filtering (both the plain `content` format and the old priority embed format via `parsePriorityEmbedEvent`, and that both can be parsed together from the same channel history), `parseDmEvents`'s author-identity filtering for the post-cutover priority DM thread, and `reconstructSessions`'s single-open-slot-per-name start/end pairing — including concurrent different-member lives, unmatched start/end edge cases, the duplicate/orphaned-start regression (a second "start" for the same name before its "end" must NOT get paired with a much-later "end", see §10's ninth bug), and the `MAX_PLAUSIBLE_LIVE_DURATION_MS` sanity-cap filtering into `discardedSessions`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `tests/idnApi.test.js`              | `isJkt48Member`'s username-prefix check, plus (§10's forty-second/forty-sixth items) `fetchPublicProfileByUsername`'s success/not-found/HTTP-error/GraphQL-error shapes (including IDN's real "User Not found" GraphQL error being `null` and a mixed error list still throwing), all against a mocked `global.fetch` — no real IDN network calls in the suite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `tests/schedulePattern.test.js`     | (§10's forty-third item) `computeSchedulePattern`'s bucket/weekday math against hand-built entries (including the "malam" bucket's midnight-wraparound case, asserting the range is `22`-`0`, not the nonsensical `"00-23"` a naive min/max would produce) and its `< 3` entries `null` case, plus `isHourInRange`'s normal and wraparound-range handling — all pure, no clock/storage dependency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `tests/headsUpAlerts.test.js`       | (§10's forty-third item) `storage/headsUpAlerts.js`'s per-username, per-WIB-day dedup: unmarked defaults to not-alerted, marking then checking the same day reads back `true`, checking a _different_ day after marking reads back `false` (the daily auto-reset), and two different usernames don't cross-contaminate each other's marked state.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `tests/priorityDm.test.js`          | (§10's forty-third item — this module had no prior test coverage at all, since exercising it means faking a discord.js `Client`) `maybeSendHeadsUpAlerts` end to end via a fake `{ users: { fetch } }` client (`discordClient.js`'s exports reassigned before a fresh re-require of `priorityDm.js`): a strong, in-window pattern sends exactly one DM; a second call the same day sends none (daily dedup); an already-live member is skipped even with a matching pattern; fewer than 5 tracked lives is skipped; a scattered/non-dominant pattern (no single time bucket reaching 50%) is skipped; and the current hour falling outside the predicted window is skipped.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `tests/compareFlow.test.js`         | (§10's forty-fourth/forty-sixth items) the bare-"cok bandingin" dropdown/search flow via lightweight fake `interaction` objects (same pattern as `tests/menu.test.js`, `global.fetch` mocked with IDN's real "User Not found" error shape): the entry reply's buttons, "close" via `deleteInteractionMessage`, both search buttons opening the right modal (member A's username carried into B's), the modal-submit branches - an unknown name ("gak nemu member JKT48"), a real never-live member such as Kimmy ("belum pernah live"), exactly one match auto-advancing, several matches becoming a dropdown - step B rejecting member A with the plain "member yang sama" message (and `embeds` absent) instead of "gak ketemu", dropping A from a shared-prefix match, never-live members explained in step B too, both the auto-pick and dropdown-select paths ending in the same comparison reply with a "Tutup" button, and a same-member select being refused.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `tests/repeatedReplyGuard.test.js`  | (§10's forty-fifth/forty-sixth items) `registerExchange`'s pure streak logic with injected `now`: sends 1-2 leave everything alone, send 3 deletes all earlier typed messages AND bot replies, later sends delete only the newest earlier pair, a different text resets the streak, different authors/channels never affect each other, and a gap past `REPEAT_WINDOW_MS` starts a new streak (while exactly at the boundary still continues); plus `pruneRepeatedExchange` calling `channel.messages.delete` for every stale ID, never throwing when a delete fails, ignoring "already deleted" (`10008`) silently and logging the missing-"Manage Messages" (`50013`) explanation only once.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

**Isolation**: `tests/helpers/setupTestEnv.js` points `CACHE_DIR` at a fresh OS temp folder (and blanks `DISCORD_BOT_TOKEN`/`PRIORITY_PING_USER_ID`/`BOT_CHANNEL_ID`) _before_ any `src/` module is required, so tests never read or write the real `data/` folder. It must be the first `require` in any test file that touches storage — `src/config.js` is a singleton cached by Node's `require`, so if some other module reads it first, the temp `CACHE_DIR` override arrives too late. `node --test` runs each test file as its own process, so this isolation doesn't leak between files.

**A test caught a real bug while this suite was being written**: `getHourWIBOf()` used `Intl.DateTimeFormat({ hour12: false })`, which has an ICU quirk — it returns `"24"` instead of `"0"` for the _entire_ 00:00–00:59 WIB hour, not just the instant of midnight. `notify/publicAlerts.js`'s `maybeSendDailyRecap()` checks `getHourWIBOf() < DAILY_RECAP_HOUR` (default `23`) to skip early; during that hour, `24 < 23` is `false`, so it fell through, found no sessions yet (the day had just rolled over), skipped posting — but still stamped `recapSentDate = log.date`, silently marking that day's recap as already sent before it had even started. The real 23:00 recap for that day would then never fire, with no error anywhere. Fixed with `% 24` in `utils.js`'s `getHourWIBOf()`; guarded by the `tests/utils.test.js` regression test that checks every minute of that hour explicitly.

**A second and third bug, reported by the owner directly** (a member who was still live wasn't showing up in `cok rekap` — twice, even after the first fix): `daily-log.json` used to track "open" sessions (`endedAtUnix === null`) as a second representation of "is member X live," parallel to `active-lives-cache.json`. The day-rollover logic dropped _every_ session on a WIB date change, including open ones - a live spanning midnight would vanish from the recap until it ended. That was patched twice (carry over open sessions across rollover, then a poll-cycle self-heal reconciliation) before the actual root cause was addressed: **`daily-log.json` no longer stores open sessions at all.** `active-lives-cache.json` is now the single source of truth for "who's live right now"; `daily-log.json` is a plain append-only archive of completed sessions, written once via `recordLiveEnded()` when a live ends. Anything needing "today's full picture" merges the two at read time (`chat/replies.js`'s `getTodaySessionsForRecap()`) - there's no second copy of live-tracking state left to drift out of sync. Guarded by `tests/dailyLog.test.js` and the merge test in `tests/replies.test.js`.

**A fourth bug report turned out not to be a bug**: the owner noticed `cok rekap minggu ini`/`cok rekap bulan ini` only showing the last few days despite the bot running much longer, and suspected the monthly recap was reusing the weekly one's filtering logic. `getCompletedSessionsSince`'s rolling-window math and the router's `minggu`→7/`bulan`→30 dispatch were both re-verified correct. The actual cause, found via `git log` on `dailyLog.js`: the cross-day archive this feature depends on didn't exist yet for most of the bot's history — `daily-log.json` reset its `sessions` array on every WIB midnight from the bot's first commit until the data-model unification rewrite above, an hour before the weekly/monthly recap feature was even added. Nothing from before that rewrite could ever have survived into the archive, so both windows were - correctly - capped at the same earliest date, which is why they looked like they shared logic. Since that history genuinely can't be recovered from the bot's own data, the fix isn't in the filter (nothing was wrong there): `getEarliestSessionDate()` lets `replyRecapRange()` detect when the archive's oldest record is more recent than the requested window and append a note explaining why, instead of a short recap silently looking broken. Separately, the owner found that Discord itself still had every notification message the bot had ever posted to the channel, going back to day one — so `scripts/backfill-live-history.js` (§9) reconstructs the missing sessions from that message history and backfills them, closing the gap for real rather than just explaining it away.

**A fifth and sixth bug were reported together, right after the backfill above shipped**: (1) `"Lily berapa kali live?"` replied `"Lily lagi nggak live sekarang"` instead of a count — `router.js`'s `liveCountMatch` only had one direction, `"berapa kali <nama> live"` (keyword before name); the user's phrasing put the name first, so it fell all the way through to the `findDurationHistoryByNameFragment` fallback. Fixed by adding a second, name-first alternative — matched against a `commandText` with the leading `"cok"` wake word stripped first, since an unanchored capture group placed before a literal keyword (unlike every other command's keyword-first patterns) would otherwise swallow `"cok"` into the captured name. (2) `"cok kapan lily live?"` only showed 1 tracked live despite 3 known from the backfilled history — the backfill endpoint (previous bug entry) only ever wrote to `daily-log.json` and rebuilt `live-count.json`; it never touched `live-duration-history.json`, which is what `cok stats`/`cok kapan ... live` actually read. Fixed by also calling a new `recordLiveDurationAt()` (§4) for each accepted session, using the session's real historical end time rather than "now" (which `recordLiveDuration` always uses) so the reconstructed entries don't distort the day/time-of-day pattern `replySchedulePattern` computes from them. A follow-up fix decoupled that duration-history write from daily-log's cutoff entirely (dedup by exact `at` already present per username, independent of daily-log's own cutoff) — the first version reused the same cutoff-filtered list for both, which meant a user who'd already run `--apply` once before this fix existed couldn't fix it by simply re-running: daily-log's cutoff had already moved past those sessions.

**A seventh bug, and the most consequential one**: the owner correctly guessed the root cause themselves - priority members (Nala/Levi/Lily/custom) still had zero backfilled history, and they suspected it was because "the notification type is different" for them. They were right. `git log` on the priority code confirms it: before commit `0bd317e` (2026-09-18 11:40 WIB - notably, _after_ several days of the bot already running), priority-member channel notifications used the flashy **embed** format (`priority/index.js`'s `buildPriorityPayload` - colors, a link button, member name inside `embeds[0].description`/title, not in `content`) posted to the same public channel; that commit is what moved the flashy version to a DM-only feature and made the channel always use the plain `buildNormalPayload` text format for everyone. `scripts/backfill-live-history.js`'s parser only ever checked `message.content` against the plain format's regex, so **every priority-member live from before that commit was silently invisible to it** - not a small gap, since Nala/Levi/Lily are exactly the members most likely to have been live early and often. Fixed with `parsePriorityEmbedEvent()`, a second detector that reads `message.embeds[0]` (title/description/url) for the old format and feeds the same `{type, name, username, atMs}` shape into the same event list - `reconstructSessions()` needed no changes at all, since pairing logic is format-agnostic once the two parsers normalize to the same event shape.

**An eighth bug, the other half of the same story**: even after the seventh fix, priority-member history from _after_ 0bd317e (2026-09-18 11:40 WIB onward - the majority of the bot's runtime by then) was still missing. The commit above didn't just change the embed's format, it changed its **destination**: `notify/priorityDm.js`'s `sendPriorityDM()` sends the flashy "JANGAN SAMPE KETINGGALAN!" embed as a **DM** (`client.users.fetch(PRIORITY_PING_USER_ID).send(...)`) straight to the owner, never through the shared webhook channel at all. `scripts/backfill-live-history.js` only ever fetched the one guild channel resolved from `DISCORD_WEBHOOK_URL` - that DM thread was a second, entirely separate message history it never looked at. Fixed by adding `parseDmEvents()`, which fetches the bot's own DM channel with `PRIORITY_PING_USER_ID` (`user.createDM()` + the same paginated `fetchAllMessages()` used for the guild channel) and reuses `parsePriorityEmbedEvent()` against it - the DM always uses the embed format, so no third parser was needed, just a different "who do I trust this message from" check (`message.author.id === client.user.id`, since DMs aren't sent via webhook and have no `webhookId`). `main()` now merges channel events and DM events into one sorted list before `reconstructSessions()` runs, so a member's start (DM, post-0bd317e) can still pair correctly with their end (also DM) or, for a live spanning the cutover, even one of each kind.

**A ninth bug, and the most severe one yet**: the owner reported sessions with durations over 100 hours showing up across the recap _and_ `live-count`/`live-duration-history` - not a small handful, but "basically every member's numbers are broken." Root cause: `reconstructSessions()`'s pairing was a FIFO **queue** per member name - every unmatched "start" got pushed onto an array, and each "end" always shifted off the _oldest_ one. That's correct only if starts and ends for a given member always alternate perfectly. They don't always: if a "start" notification ever got sent twice for the same live (e.g. the bot restarting mid-live and re-detecting the member as newly live - exactly the kind of duplicate notification this project has hit before, see the crash-recovery/graceful-shutdown work in §9), the queue grows to length 2, and the next "end" pairs with the _wrong_ (older, already-closed) start - producing a session that spans from some earlier live all the way to this end, sometimes days later. Worse, this is a **cascading** bug: the newer, unconsumed start stays queued for the _next_ end, so a single duplicate near the start of a member's history can poison every session reconstructed for them afterward, which matches what the owner saw ("many members, durations totally broken"). Fixed in two layers: (1) `reconstructSessions()` now tracks **one open session per name**, not a queue - a new "start" while one is already open closes out (orphans) the old one into `unmatchedStarts` instead of leaving it to be mismatched later, so an "end" always pairs with the _most recent_ start; (2) a new `MAX_PLAUSIBLE_LIVE_DURATION_MS` sanity cap (`config.js`, 12 hours - generous given real sessions here are minutes to a couple hours) is checked in three independent places - client-side in `reconstructSessions()` (discarded into a new `discardedSessions` list, logged so the gap is visible), server-side in `handleBackfillLiveHistory`'s `validSessions` filter (defense in depth against any future data source), and retroactively via a new **repair** endpoint/script for data that was already written to Railway before this fix existed (`POST /api/repair-live-history`, `src/server.js`'s `handleRepairLiveHistory` - strips implausible-duration entries from `daily-log.json` and `live-duration-history.json`, then rebuilds `live-count.json` from what's left; `scripts/repair-live-history.js`, same manual-CLI/dry-run-by-default pattern as `backfill-live-history.js`). The owner needs to run `npm run repair-live-history -- --apply` once against the already-corrupted Railway data - the reconstruction fix alone only prevents _new_ corruption, it can't retroactively un-corrupt what an earlier `--apply` run already wrote.

**A tenth bug, found by the owner investigating the ninth**: while checking how many priority "JANGAN SAMPE KETINGGALAN!" notifications existed (`scripts/count-priority-notifs.js`, a new read-only script - raw keyword search on `message.content` plus a structured `parsePriorityEmbedEvent` parse, cross-checked against each other), a literal keyword search found 10 matching messages in the channel, but the structured parse found **zero**. `git log -S` on the description string (`src/priority/index.js`) turned up a second format change that the seventh-bug fix hadn't accounted for: `embeds[0].description` itself changed shape twice, not once. From the priority feature's introduction (`73ddeba`, 2026-09-13 19:13 WIB) until `b595d90` (2026-09-17 21:49 WIB, when the manual "TONTON SEKARANG" link was replaced by a real button), the description was `**Name** baru aja mulai live di IDN Live.\n\n[🔴 **TONTON SEKARANG**](url)` - link text and all. Only after `b595d90` did it shrink to just `**Name** baru aja mulai live di IDN Live.`, which is the only shape `PRIORITY_START_DESC_RE` was ever written to match (it anchored with `$` right at the end of that sentence). Since JS's `$` without the `m` flag matches only the very end of the whole string, the regex silently rejected every message from those first ~4 days - not a partial miss, a **complete** one for that entire window, which is exactly the period right after the bot went live when priority members would have been getting checked most. Fixed by dropping the trailing `$` anchor (the leading `^` plus the fixed prefix is enough to identify the format; anything after "di IDN Live." is now ignored rather than required to be absent). `scripts/count-priority-notifs.js` also got a `unparsed` dump - keyword matches that still fail structured parsing print their raw `content`/`embed.title`/`embed.description`/`embed.url` instead of silently vanishing from the count, so a future format change surfaces the same way this one did rather than needing another owner-side investigation to notice. Since this regex is shared with `backfill-live-history.js`'s actual reconstruction, any priority-member sessions from that ~4-day window were also silently absent from a prior `--apply` run - re-running `npm run backfill-live-history -- --apply` after this fix deploys should pick them up (idempotently, same as always - no `repair` step needed here, since this was a coverage gap, not corrupted data like the ninth bug).

**An eleventh bug/gap, a UX report rather than a data bug**: the owner relayed a user question about `cok rekap minggu ini`/`cok rekap bulan ini`'s paginated table - "isn't there a way to go back a page?" There wasn't. `pendingRecapPage` only ever stored `nextPage`, and was only set when `buildRecapTablePage`'s `hasMore` was true (i.e. there was a page after the current one) - so once someone paged forward to the _last_ page, `hasMore` became `false`, no pending state was created for it, and there was no way to navigate backward from there (or from any page, since "backward" was never implemented at all - the only recognized reply was "y" for forward and "n" to cancel, both via the existing `YES_PATTERN`/`NO_PATTERN`). Fixed by reworking the pending state to store `currentPage`/`totalPages` and stay alive for as long as `totalPages > 1`, regardless of `hasMore` - so it now exists on every page of a multi-page recap, including the last one. Added a new `PREV_PAGE_PATTERN` (`utils.js` - "mundur"/"balik"/"kembali"/"sebelumnya"/"prev"/"previous"/"back") kept deliberately separate from `NO_PATTERN`, since `chat/menu.js`'s watch-confirm flow also reuses `YES_PATTERN`/`NO_PATTERN` for an unrelated y/n question ("mau nonton?") where "n" has to keep meaning "cancel," not "go back a page." `tryHandleRecapPageShortcut` now branches on next/prev/stop, and answers "y" past the last page or "mundur" before the first with a plain-language explanation instead of silently no-op'ing or falling through to the wake-word gate. The per-page footer text also changed to name whichever direction(s) are actually available on that page, so most users won't need to ask whether going back is possible at all. A follow-up request added `NEXT_PAGE_PATTERN` ("maju"/"forward"/"lanjut"/"next"/"berikutnya") as a more explicit alternative to bare "y" for the forward direction — additive, not a replacement, so "y" keeps working exactly as before.

**A twelfth bug, the deepest one in this whole backfill saga**: after the seventh/eighth/tenth-bug fixes (priority embed parsing, DM history, the description-format regex) all landed, the owner asked why Nala/Levi/Lily's pre-2026-09-19 lives _still_ never showed up in `cok rekap` even though `count-priority-notifs.js` could now find and parse the notifications correctly. The parsing was fine - the problem was `handleBackfillLiveHistory`'s idempotency check itself. It used a **date cutoff**: `getEarliestSessionDate()` (the oldest `endedAtUnix` currently in `daily-log.json`) as a hard line, accepting only sessions ending before that line. That's fine for a _single_ backfill run filling a gap once - but this project ended up needing **multiple** backfill runs over the _same_ early period, as each parser bug was found and fixed in turn (bugs seven, eight, and ten all touched sessions from the same 2026-09-13 to 2026-09-18 window). The first successful run (even backfilling just a handful of non-priority sessions) pulled the cutoff forward to cover that whole window - so every subsequent run, no matter how correctly it now parsed the priority notifications, had every one of its newly-found sessions rejected outright, because their `endedAtUnix` fell "after" a cutoff that had already swallowed the entire period. This is exactly why the ninth/tenth-bug fixes could be verified as _parsing_ correctly (via `count-priority-notifs.js`, which never touches the cutoff) while still never actually reaching the recap. Fixed by replacing the whole cutoff concept with **per-session dedup**: before inserting, every incoming session is checked against existing `daily-log.json` entries for the same `username` with an `endedAtUnix` within `SESSION_DEDUP_TOLERANCE_SEC` (120 seconds - `src/server.js`, generous enough to absorb the few seconds of drift between the live-tracker's `Date.now()` at detection time and a Discord message's `createdTimestamp`, but far shorter than the hours/days between any two genuinely different lives of the same member). A session either matches something already there (skipped, no matter its date) or it doesn't (accepted, no matter its date) - there's no longer any global "boundary" for new discoveries to fall on the wrong side of. `cutoffDateWIB` was dropped from the endpoint's response entirely, since it no longer means anything.

**A thirteenth and fourteenth bug, caught by a full professional-style audit rather than a user report**: the owner asked for a systematic bug sweep of the whole codebase. Two real correctness issues turned up. (13) `storage/jsonStore.js`'s `save()` updated its in-memory `cache` **before** attempting the disk write, not after - if `fs.writeFileSync` ever threw (disk full, permission error, Volume detached), the function logged the error and returned, but the process kept believing the new value was persisted (every `load()` afterward, for the rest of that process's life, would keep returning data that was never actually written to disk). A restart at that point would silently revert to the last value that truly made it to disk, discarding everything the bot "thought" it had saved in between - with no error visible anywhere pointing at _that_. Fixed by moving the `cache =`/`hasCache =` assignment to run only after a successful write. (14) `monitor.js` only called `saveActiveLives()` conditionally - inside `maybeAlertEndingSoon`/`maybeAlertViewerMilestone`, and only on the specific poll cycle where one of those thresholds actually fired - even though `viewCount`/`peakViewCount` are updated on the in-memory entry every single cycle. A live that never crossed a fresh milestone (or already had its `endingSoonAlerted` flag set) could run for a long time without `active-lives-cache.json` ever being rewritten; a Railway restart during that window would resume from a stale, lower `peakViewCount`, silently undercounting "paling rame ditonton" for any live that happened to span a redeploy. Fixed by saving unconditionally at the end of that branch, every cycle - the file is tiny (a handful of active lives at most), so the extra writes are not a meaningful cost.

**A fifteenth bug, found by a follow-up audit pass specifically looking for a repeat of the ninth bug's implausible-duration class through a different door**: the ninth bug's fix added `MAX_PLAUSIBLE_LIVE_DURATION_MS` sanity checks to `handleBackfillLiveHistory`/`handleRepairLiveHistory` (server.js) and to `reconstructSessions()` (the backfill script) - three places, all on the **backfill** path. `monitor.js`'s normal, always-on "a live just ended" path (the one that runs every 30 seconds while the bot is up) never got the same check: `durationMs = Date.now() - new Date(memberData.liveAt).getTime()` was written straight to `daily-log.json`/`live-duration-history.json`/the new-record announcement with no upper bound at all. `active-lives-cache.json` (the only source for `liveAt`) is loaded once at boot and persists across restarts by design (so a live still in progress isn't re-announced as a duplicate "start" after a redeploy) - but that same persistence means a `liveAt` can go stale if the bot is down for an extended stretch (a crash loop, a Railway incident, anything longer than a routine redeploy) while a member happens to still be live when it comes back. The very first poll after such a restart would compute a duration spanning the entire downtime and write it as if it were a real, continuously-observed session - reproducing the "100+ hour live" bug for real usage, not just backfilled history. Fixed by applying the same `durationMs > 0 && durationMs <= MAX_PLAUSIBLE_LIVE_DURATION_MS` guard here too: an implausible session is discarded from stats/records/the daily log entirely (there's no way to tell how much of the measured span was real live time vs. downtime, so no attempt is made to "clamp" it to something plausible) but the "selesai" notification still goes out and the stale entry is still removed from `active-lives-cache.json`, so it can't get stuck. Guarded by `tests/monitor.test.js`, which calls the real `checkLiveMembers()` with `global.fetch` mocked (no real network) against a manually seeded stale `activeLives` entry.

**A sixteenth bug, the most severe found by this audit**: none of the bot's outgoing Discord messages ever set `allowed_mentions`/`allowedMentions`, and Discord's default when that's omitted is to parse and trigger **every** mention it finds in a message's `content` - `@everyone`, `@here`, and roles included, regardless of who or what put that text there. Several chat replies echo a caller-supplied fragment straight back into `content` - `replyMemberNotFound`, `replyMemberStats`, `replyLiveCount`, `replyGifterSnapshot`, `replySchedulePattern`, and `replyMySubscriptions` (`chat/replies.js`) all do this - and the subscribe command that feeds several of them (`"cok ingetin <apa aja>"`, `handleSubscribe`) is **not** owner-gated, unlike the priority-list commands. That meant any member of the server could type `cok ingetin @everyone` and have the bot's own confirmation reply (`Sip, kamu bakal di-tag tiap kali "@everyone" mulai live!`) genuinely ping the whole server the moment it was sent - and every later `"cok reminder aku"` would re-trigger it again from the stored subscription. The same gap existed on the channel-webhook side (`notify/webhook.js`'s `postToWebhook`): any payload whose text happened to contain `@everyone`/`@here`/a role mention - most plausibly an IDN member's own display `name`, which flows unescaped into nearly every notification - would ping on send with no code path involved at all. Fixed with a default-deny policy at the two places all outgoing messages funnel through, rather than patching each call site's text: `postToWebhook` now wraps every payload in `allowed_mentions: { parse: [] }` unless the payload sets its own (`withDefaultMentionGuard`, `notify/webhook.js`) - the one legitimate case that needs to ping someone, the subscriber `<@id>` mentions appended in `notify/liveNotify.js`'s `sendDiscordNotif`, now sets `allowed_mentions: { users: subscriberIds }` explicitly, so only those exact IDs can ever be pinged from that message regardless of what else is in the text. `chat/router.js`'s `message.reply()` and every `interaction.reply()` in `chat/menu.js` now go through a new `safeReplyOptions()` (`utils.js`), which does the equivalent for the bot-client side (`allowedMentions: { parse: [] }`) - centralized at the send call sites rather than in each reply-builder, so a future reply function can't reintroduce the gap by forgetting to sanitize its own text. DMs (`notify/priorityDm.js`, `notify/crashAlert.js`) were left as-is - `@everyone`/`@here`/roles have no meaning outside a guild channel, so there's nothing for this class of bug to exploit there. Guarded by `tests/utils.test.js` (`safeReplyOptions`), `tests/webhook.test.js` (`withDefaultMentionGuard` and `postToWebhook`'s actual outgoing body, via a mocked `global.fetch`), and updated assertions in `tests/menu.test.js` confirming the wrapped shape reaches `interaction.reply()`.

**A seventeenth bug, found while writing a test for the new daily-recap-embed feature below**: `config.js` read two numeric env vars (`DAILY_RECAP_HOUR`, `ENDING_SOON_THRESHOLD_MINUTES`) with the pattern `Number(process.env.X) || fallback`. That pattern has a classic JS footgun - `0 || fallback` evaluates to `fallback`, because `0` is falsy - so an env var deliberately set to `"0"` (e.g. wanting the daily recap to fire right at WIB midnight, or wanting the ending-soon heuristic to fire immediately with no threshold) was silently overridden back to the default, with no error or log pointing at why. Caught because a test for `maybeSendDailyRecap()` needed `DAILY_RECAP_HOUR=0` specifically to make the function's hour gate (`getHourWIBOf() < DAILY_RECAP_HOUR`) deterministically always pass regardless of the real time the test happened to run at - the test failed with the recap never firing, which traced back to `DAILY_RECAP_HOUR` silently still being `23`. Fixed with a small `envInt(name, fallback)` helper in `config.js` that only falls back when the env var is genuinely unset or an empty string, or fails to parse as a finite number - a real `"0"` now stays `0`. Guarded by `tests/config.test.js`, which re-requires `config.js` fresh (clearing `require.cache`) under several `DAILY_RECAP_HOUR`/`ENDING_SOON_THRESHOLD_MINUTES` values (unset, `"0"`, empty string, non-numeric, a normal value) and asserts the resulting exported constant each time.

**An eighteenth bug, reported by the owner with a recap-table screenshot**: the same member appeared twice in `cok rekap`'s table with the **exact same** "Mulai" time but two different (short, then longer) durations - e.g. Erine at `15.50 WIB` for `1m`, then Erine again at `15.50 WIB` for `1j 11m` right after. Root cause: `monitor.js`'s "a live just ended" loop treated a member missing from a single IDN API response as an immediate, final "ended" - but IDN occasionally returns a `200 OK` response that simply omits a live that's still genuinely running (a glitch on IDN's side, not a network error, so it never hits the outer `try/catch`). That false "end" fired an end notification and recorded a short session; the very next 30-second poll saw the member live again and treated it as a **new** start - but since IDN's `live_at` for a given broadcast doesn't change, the "new" session's start time was identical to the one that had just (wrongly) ended, producing exactly the paired rows the owner saw. Fixed with `ENDED_GRACE_POLLS = 2`: a member now has to be missing from IDN's response for **two consecutive** poll cycles (~1 minute) before being treated as actually ended, tracked via a new `missingStreak` counter on the `activeLives` entry that resets to `0` the moment the member is seen again (so a real, continuous live is never split, and no duplicate "start" is ever sent for it - the original entry, including its original `liveAt`, is simply kept). A genuine end is now detected up to one extra ~30-second poll cycle later than before, which is an acceptable trade-off for not fragmenting real sessions. Guarded by two new `tests/monitor.test.js` cases that call the real `checkLiveMembers()` across several consecutive cycles (via a `global.fetch` mock that simulates IDN's own paginated response shape) - one confirms a single miss is tolerated and the session stays continuous with its original `liveAt`, the other confirms two consecutive misses still correctly end and record the session.

**A nineteenth bug, self-found while reviewing the "Mulai jam"/"Selesai jam" notification feature above**: `idnApi.js` passes `live_at` through completely unvalidated - it's whatever the IDN GraphQL response happens to contain. `sendDiscordNotif` (`notify/liveNotify.js`) used it directly as `new Date(liveAt)`, and if that string were ever something `Date` can't parse (not just `null`/empty, which was already handled), the result is an Invalid Date - and `formatClockWIB` (`utils.js`) calls `Intl.DateTimeFormat.format()` on it, which **throws** a `RangeError` on an Invalid Date rather than producing garbage text (unlike `formatDuration`/`describeElapsed`, which just render `NaN` harmlessly). That throw happens before `sendDiscordNotif`'s own logic can recover, propagates out of `checkLiveMembers()`'s per-member loop in `monitor.js`, and is only caught by that function's outermost `try/catch` - meaning a single malformed timestamp from IDN would abort the rest of that entire poll cycle, skipping every other member still left to process in it, not just the one with the bad timestamp. The same unvalidated-`liveAt` pattern existed in `chat/replies.js`'s `replySpecificMember` (used by "cok siapa yang live <nama>"), where it would instead be silently swallowed by `chat/router.js`'s `messageCreate` try/catch - the user would just get no reply at all. Fixed in both places by parsing `liveAt` once and checking `!Number.isNaN(parsed.getTime())` before treating it as valid, falling back to "now" (`sendDiscordNotif`) or omitting the start-time clause entirely (`replySpecificMember`) otherwise - never passing a potentially-invalid `Date` into `formatClockWIB`. Guarded by new cases in `tests/liveNotify.test.js` (`sendDiscordNotif` called with a garbage `liveAt` string and with `null`, both asserted not to reject and to still produce a "Mulai jam" line falling back to the current time) and `tests/replies.test.js` (`replySpecificMember` called with a garbage `liveAt` and with `null`, both asserted not to throw, the garbage case additionally asserted to omit the "mulai jam" clause rather than render corrupted data).

**A twentieth item, prompted by the owner reporting notifications "sometimes don't come out right away"**: not a bug — analysis concluded the dominant cause is Discord-side (a channel's notification setting defaulting to "Only @Mentions" means a plain, unmentioned notification triggers no push/badge at all; mobile push delivery itself is also inherently variable, outside the bot's control) — but two real, if secondary, contributors were found and fixed on the code side. First, `pollLoop` (`monitor.js`) used to wait the full `POLL_INTERVAL_MS` **after** `checkLiveMembers()` finished, rather than keeping a fixed cadence from the start of each cycle - a slow cycle (e.g. one that hit Discord's 429 retry/backoff) pushed out every cycle after it too, compounding delay rather than staying isolated to the one slow cycle. Fixed by extracting `computeNextPollDelay(elapsedMs)` as a pure, directly-testable function - `POLL_INTERVAL_MS - elapsedMs`, floored at a `MIN_POLL_DELAY_MS` (1 second) so a cycle that ran as long as or longer than the interval itself still gets _some_ gap rather than hammering IDN's API back-to-back. Second, the poll interval itself is now configurable (`POLL_INTERVAL_SECONDS`, `config.js`'s `envInt`) and its default lowered from 30 to 20 seconds, shrinking the baseline detection-lag floor while leaving the actual trade-off (faster detection vs. more IDN API load) tunable by the owner without a code change. Also added: a tip in `replyHelp()` pointing owners/members at the "All Messages" vs. "Only @Mentions" Discord channel setting directly, since that's the most likely actual fix for any individual person experiencing this. Guarded by new cases in `tests/monitor.test.js` (`computeNextPollDelay`), `tests/config.test.js` (`POLL_INTERVAL_MS` env var parsing), and `tests/replies.test.js` (`replyHelp` contains the tip).

**A twenty-first item, an owner feature request rather than a bug**: recap pagination (`cok rekap`/`cok rekap minggu ini`/`cok rekap bulan ini`) could only be navigated by typing "y"/"maju"/"mundur"/"n", even though the codebase already had button infrastructure elsewhere (`chat/menu.js`'s fallback menu) that was never applied here. This also changed `buildRecapPageBlock`'s return type — previously a plain string, now `{ content, components }` — which propagates through `replyTodayRecapSoFar`/`replyRecapRange`/`tryHandleRecapPageShortcut` (whenever the latter renders an actual page rather than one of its plain-string boundary/stop messages) and needed every test asserting on those functions' return values updated to check `.content` instead of the bare string; every `src/` call site already just returns the value through (`safeReplyOptions` in `chat/router.js` already accepts a string or an object), so nothing there needed to change. See the new "Recap navigation also has a button path..." paragraph in §5 for how the button/modal mechanism itself works.

Guarded by new cases in `tests/replies.test.js`: `handleRecapNavButton`'s "next"/"prev" actions re-rendering the correct target page from a self-contained `customId` (independent of `pendingRecapPage`), "close" editing in the fixed thank-you text and also clearing `pendingRecapPage` so a stray typed "y" afterward doesn't silently resume a "closed" recap, and "search" calling `interaction.showModal()` rather than `interaction.reply()`; `handleRecapSearchModalSubmit` finding a member's sessions and excluding others, a not-found query producing an explicit "gak nemu" message rather than an empty table, and the `"today"` range encoding pulling from `getTodaySessionsForRecap()` rather than `getCompletedSessionsSince()`; plus exact-`customId` assertions on `buildRecapPageBlock`'s button row for the first-page (`next` + `close` + `search`, no `prev`), middle-page (all three plus `search`), and last-page (`prev` + `close` + `search`, no `next`) cases, matching the owner's exact spec for which buttons appear where.

**A twenty-third item, reported the same day the buttons above shipped**: clicking "Maju"/"Mundur" posted a brand new table message every time rather than replacing the one the button was attached to, so a few clicks left several near-duplicate tables stacked in the channel — wasted space, and confusing about which one was "current." `handleRecapNavButton`'s "next"/"prev"/"close" branches used `interaction.reply()`, Discord's "post a new message in response" call, the same one every other button handler in this codebase uses (`chat/menu.js`) — reasonable for those (each answers a distinct question), wrong here (all of them re-show the same evolving table). Fixed by switching those three branches to `interaction.update()`, which edits the message the clicked button is attached to instead of sending a new one — same `{content, components}` payload shape, so `buildRecapPageBlock`'s output needed no change, only the Discord API call around it. "close" also now explicitly sends `components: []` so the button row disappears along with the table content, rather than leaving live-looking buttons under a "closed" message. The typed "y"/"mundur"/"maju"/"n" path (`tryHandleRecapPageShortcut`, dispatched through `chat/router.js`'s `message.reply()`) was deliberately left as-is — it has no message to "edit" in the same sense (it's a reply to whatever the user just typed, not a click on an existing component), and unifying it would mean tracking and re-fetching a specific prior message ID through `pendingRecapPage`, a meaningfully bigger change the owner didn't ask for. Guarded by updated `tests/replies.test.js` cases (a `.update` spy added to the file's fake `interaction` helper alongside the existing `.reply`/`.showModal` ones) explicitly asserting `interaction.reply()` is called zero times for "next"/"prev"/"close", `interaction.update()` carries the new page/thank-you content, and "close"'s update includes `components: []`.

**A twenty-fourth item, an owner follow-up on the search feature above**: after searching for a member and getting the filtered result, the original full recap table was still sitting in the channel right above it - the owner asked why, and wanted an explicit yes/no choice about deleting it rather than it just lingering. `handleRecapSearchModalSubmit` now appends a `buildKeepOrDeleteRecapComponents(interaction.message?.id)` button row ("Ya, biarin" / "Enggak, hapus aja") whenever the search reply can identify the original table message - a `ModalSubmitInteraction`'s `.message` is populated precisely when the modal was opened from a button attached to an existing message, which is always true here (the modal only ever opens from the recap table's own "🔍 Cari member" button), but the code still falls back to no buttons/no question rather than assuming a non-null value. Both buttons carry that original message's ID directly in their own `customId` (`recap_nav:keeprecap:<id>` / `recap_nav:delrecap:<id>`) rather than relying on any pending-state map, consistent with every other `recap_nav:` button being self-contained. "Enggak, hapus aja" calls `interaction.channel.messages.delete(id)` on the original table (no need to fetch it first - deleting a single message by ID works regardless of its age, unlike `bulkDelete`'s 14-day cap) and also clears `pendingRecapPage` for that channel+author, same reasoning as "Tutup rekap" - a stray later "y"/"mundur" shouldn't try to continue paging a table that no longer exists. "Ya, biarin" does nothing destructive. Either way, the choice buttons themselves are removed via `interaction.update()` on the **search result** message (its own content is kept, only `components: []` changes) so the question can't be answered twice. Guarded by new `tests/replies.test.js` cases: the question/buttons appearing (with the right message ID encoded) only when `interaction.message` is set, appearing for both a found and a not-found search, `delrecap` actually calling `channel.messages.delete` with that exact ID and clearing `pendingRecapPage`, and `keeprecap` deleting nothing at all - both converging on the same "remove own buttons, keep own content" update.

**A twenty-fifth item, another owner feature request**: the fallback menu's option 4 ("cek member") - after picking a live member (via the dropdown or typing `"4 <nama>"`), the "mau nonton sekarang?" follow-up only ever accepted typed "y"/"n" (`tryHandleWatchConfirmShortcut`), same as recap pagination used to. The owner asked for buttons here too ("biar gak ribet"), plus a way to back out entirely if the wrong member got picked by mistake. `startWatchConfirmForEntry` (shared by both the dropdown and the `"4 <nama>"` text-shortcut path - one function, so both entry points got the fix at once) now returns `{content, components}` with three buttons: "🔴 Ya, nonton" / "Enggak" / "Tutup", handled by a new `handleWatchConfirmButton`. Consistent with the recap buttons' `customId` design (§10's twenty-first/twenty-third items), the username is encoded directly in the `customId` (`watch_confirm:<yes|no|close>:<username>`) rather than relying on `pendingWatchConfirm` to function - the button stays clickable indefinitely, not just within `PENDING_WATCH_CONFIRM_TTL_MS`. `pendingWatchConfirm` is still consulted, but only as a display-name fallback for the rare case where the member has already stopped being live (and so dropped out of `activeLives`) by the time the button is clicked - `activeLives.get(username)?.name` is tried first, falling back to the pending map's stored `name`, then finally a generic "member ini" if neither is available. All three actions use `interaction.update()`, not `reply()`, matching the in-place-edit convention established by the recap buttons - "close" needed no `activeLives` check at all, unlike "yes"/"no". The typed "y"/"n" path (`tryHandleWatchConfirmShortcut`) is unchanged and still works; clicking any of the three buttons clears that channel+author's `pendingWatchConfirm` entry so a stray later typed "y"/"n" can't double-answer the same prompt. Guarded by new `tests/menu.test.js` cases for each button action's `interaction.update()` content (and zero `reply()` calls), the stale-member name-fallback behavior, and pending-state clearing after a button click.

**A twenty-second item, another owner feature request**: `formatClockWIB` (`utils.js`) - the single formatter behind every "Mulai jam"/"Selesai jam" notification line (§6), the priority DM's `🕐 Mulai`/`🕐 Selesai` embed fields (`priority/index.js`), the recap table's "Mulai" column and `replySpecificMember`'s "mulai jam" clause (`chat/replies.js`) - only ever showed hour and minute. The owner asked for seconds too. Since it's one shared formatter behind all of those call sites, adding `second: "2-digit"` to its `Intl.DateTimeFormat` options fixed everywhere at once (`HH.MM WIB` → `HH.MM.SS WIB`, id-ID locale's dot separator unchanged) rather than needing a second formatter just for notifications - keeping every clock-time display in the bot consistent with itself was preferred over narrowly scoping the change to only the notification lines the request specifically mentioned. This is purely a display-format change - `daily-log.json`/`activeLives`/`live-duration-history.json` already stored full-precision timestamps (Unix seconds or ISO strings) throughout; no storage or duration-math (`formatDuration`/`describeElapsed`, which never used `formatClockWIB`) was touched. `scripts/backfill-live-history.js`'s `START_RE`/`END_RE` also needed no change, since they only anchor the start of the string ("✅ **name** udah selesai live..." / "🚨 **name** lagi live...") and never try to parse the clock-time line itself. Guarded by a new `tests/utils.test.js` case asserting the exact `HH.MM.SS WIB` shape (including that a `:00` second is padded to `"00"`, not dropped) directly on `formatClockWIB`, plus every existing test elsewhere that already asserted on notification/embed/recap clock-time text (`tests/liveNotify.test.js`, `tests/monitor.test.js`, `tests/priority.test.js`, `tests/replies.test.js`) updated from a `\d{2}\.\d{2}` pattern to `\d{2}\.\d{2}\.\d{2}`.

**A twenty-sixth item, an owner decision on the recap table format**: the owner felt the "Durasi" column (e.g. `"1j 23m"`) was less useful day-to-day than knowing the actual clock time a member's live ended, since people mainly care about start/end times rather than derived duration. `buildRecapTablePage`'s (`chat/replies.js`) last column was renamed "Berakhir" and now renders the end clock time via `formatClockWIB`, reusing the exact same cross-midnight `"(DD/MM)"` annotation the "Mulai" column already had (comparing the WIB calendar date of `endedAtUnix` against `getTodayWIB()`) rather than inventing a second convention - a live that started yesterday and ended just after midnight now gets the date suffix on whichever side of the boundary actually applies, independently. Still-live sessions (`endedAtUnix === null`) keep showing `"-"`, unchanged. `formatDuration`'s total-duration figures in the recap summary lines above the table (`replyTodayRecapSoFar`/`replyRecapRange`) were left as-is - only the per-row table column changed, not the aggregate stats. The owner also asked whether the bot could auto-detect phone vs. desktop and format the table differently per device; this was **not implemented** - a Discord bot has no reliable signal for which client a given person is reading a specific message on (there's no such field on message/interaction objects, and the nearest proxy, a guild member's `presence.clientStatus`, needs the privileged `GuildPresences` intent this bot doesn't request, only reflects which app(s) someone is currently logged into rather than which one they're viewing a given channel with, and is ambiguous for anyone with both desktop and mobile open at once). Rather than a per-device format, the table stays a single fixed-width `\`\`\``block, unchanged in that respect from before. Guarded by two new`tests/replies.test.js`cases: the header containing "Berakhir" and not "Durasi" with a completed row's end time rendered and an in-progress row still showing`"-"`, and a cross-midnight-at-the-end session getting the `"(DD/MM)"` suffix on "Berakhir" specifically (mirroring the existing "Mulai"-side cross-midnight test just above it).

**A thirty-ninth item, the first of five features the owner asked for at once ("Q2" - the owner's own quarter/period label, unrelated to the "Q2"/"Q3" codenames elsewhere in this doc for the dedicated-channel/fallback-chat features - see §6)**: the daily recap (`maybeSendDailyRecap`) had no weekly/monthly equivalent - "cok rekap minggu ini"/"cok rekap bulan ini" existed as on-demand chat commands, but nobody got a summary unless they asked. Added `maybeSendWeeklyRecap`/`maybeSendMonthlyRecap` (`notify/publicAlerts.js`), called every poll cycle right alongside `maybeSendDailyRecap` (`monitor.js`) - cheap to check even when their gate is closed, same reasoning as the daily one. The embed-building logic (total sessions/unique members/total duration/"paling lama") was identical across all three periods, so it was extracted into a shared `buildRecapEmbedPayload(completed, title)`, with `buildDailyRecapPayload`/`buildWeeklyRecapPayload`/`buildMonthlyRecapPayload` now thin wrappers supplying just the title and the period's session set - `buildDailyRecapPayload`'s own signature/behavior is unchanged, so no existing test needed to change.

Weekly fires once per WIB week, gated on it being "Minggu" (Sunday, checked via `WEEKDAY_FORMATTER_WIB` - not `Date.prototype.getDay()`, for the same server-timezone reason `chat/replies.js`'s weekday dropdown already avoids it, §10's thirty-seventh item) rather than trying to track ISO week numbers - the data itself is `getCompletedSessionsSince(7)`, the exact same rolling-7-day source "cok rekap minggu ini" already uses, so the auto-post and an on-demand ask on the same day always agree. Monthly fires on the actual last calendar day of the month (`isLastDayOfMonthWIB`, computed by checking whether "tomorrow" falls in a different WIB month - automatically leap-year-correct, no manual days-per-month table), using `getCompletedSessionsForMonth` for the month that's closing. Both dedup the same way `recapSentDate` already does for the daily one - `daily-log.json` grew two sibling fields, `recapSentWeek`/`recapSentMonth`, each stamped with the WIB date string of the day it last fired, so re-checking on every poll cycle within that same day is a no-op.

The weekday/last-day-of-month gate check and the dedup key both take an optional `now` parameter (default `new Date()`, so production behavior is unchanged) specifically so they're deterministically testable - a test can pass a known Sunday or a known last-day-of-month without needing the real system clock to cooperate, mirroring the existing `DAILY_RECAP_HOUR=0` env var trick used to make the daily recap's hour gate always pass in tests. Deliberately **not** threaded through to the session-fetching itself (`getCompletedSessionsSince`/`getCompletedSessionsForMonth` still read the real clock) - production always calls these with no argument anyway, so the two are never actually out of sync outside of tests, and retrofitting every dependency in the chain to accept an injected clock would have been a much bigger change than this feature needed. Guarded by new `tests/publicAlerts.test.js` cases: `isSundayWIB`/`isLastDayOfMonthWIB` as pure functions against several fixed dates (including a leap-year and a non-leap-year February), `buildWeeklyRecapPayload`/`buildMonthlyRecapPayload`'s embed shape, and `maybeSendWeeklyRecap`/`maybeSendMonthlyRecap` exercised end to end (mocked `fetch`) for both a passing gate (sends once, dedups on a second call for the same day) and a closed gate (sends nothing, and - important, since a half-open gate that still stamps the dedup key would silently skip the _next_ legitimate occurrence too - leaves `recapSentWeek`/`recapSentMonth` untouched). `tests/server.test.js`'s `/api/backup` shape assertion was updated to include the two new `dailyLog` fields.

**A fortieth item, the second of the five "Q2" features**: `"cok siapa yang paling lama live"`/`"cok siapa yang paling rame ditonton"` only ever answered for "hari ini" - no way to ask "who was longest/most-watched this week/month/on a given date" without going through the full `cok rekap` table just to eyeball it. `replyLongestLive`/`replyTopViewers` (`chat/replies.js`) were split into a thin `(rangeDays, label)` wrapper - `replyLongestLiveForRange`/`replyTopViewersForRange` - reusing `getSessionsForRange`, the exact same range-resolving function `cok rekap` already uses, so "paling lama live bulan ini" and "rekap bulan ini" are guaranteed to agree on which sessions count. `replyLongestLive()`/`replyTopViewers()` themselves are now just `replyLongestLiveForRange(null, "hari ini")`/`replyTopViewersForRange(null, "hari ini")` - same output, so no existing test needed to change.

One wrinkle `getSessionsForRange` didn't already solve: a still-ongoing session (from `getOngoingSessionsForRecap`) has `durationMs: null` by design (its duration isn't final yet - fine for a recap table, which just shows `endedAtUnix: "-"`, but useless for a "who's _longest_" comparison, which needs an actual number). `replyLongestLiveForRange` computes it on the fly from the session's own `startedAtUnix` (`Date.now() - startedAtUnix * 1000`) rather than re-fetching `activeLives` separately - the ongoing session object already carries everything needed. `peakViewCount` needed no equivalent fix, since `getOngoingSessionsForRecap` already populates it directly from `activeLives`.

A new `resolveStatRangeFromText(text)` (`chat/replies.js`) recognizes the same range phrasing "cok rekap" does - a specific date (`parseSpecificDateFromText`), a bare month name (`parseMonthOnlyFromText`), or the words "minggu"/"bulan" - and returns `{ rangeDays, label } | null`, `null` meaning "no range mentioned, caller defaults to today." Deliberately **narrower** than the rekap dispatch it borrows from in two ways: it never offers a month/date _picker_ (a single-line stat answer has nowhere to attach a dropdown to), so a bare "bulan" with no specific month name always resolves straight to the current month rather than asking which one; and it doesn't support weekday names ("senin" etc.) at all - resolving "which Monday" needs the same dropdown `chat/replies.js`'s weekday picker already provides for the table view, and wiring that into a single-sentence stat answer was judged out of scope for what was asked. `chat/router.js`'s existing "paling lama"/"paling rame ditonton" trigger-phrase detection is untouched - `resolveStatRangeFromText` only runs _after_ one of those phrases is already confirmed present, and its `null` result falls through to the exact same `replyLongestLive()`/`replyTopViewers()` call as before, so every previously-passing case (including "hari ini" explicitly) is unaffected. Guarded by new `tests/replies.test.js` cases (`resolveStatRangeFromText`'s four resolvable shapes plus its `null` case, `replyLongestLiveForRange`/`replyTopViewersForRange` merging an in-progress session for the current month and computing its live duration correctly, and the empty-range "belum ada data" message) and new `tests/router.test.js` dispatch cases for "paling lama live minggu ini" and "paling rame ditonton bulan ini".

**A forty-first item, the third of the five "Q2" features, with two constraints the owner set up front**: "cok rekap ..." only ever rendered as a Discord message - useful to glance at, but nothing a user could take into a spreadsheet. The owner asked for a CSV export, with two explicit conditions: it shouldn't cost meaningful storage, and reuse whatever range-phrasing was already built rather than inventing a fourth parser. `chat/replies.js`'s new `replyExportRecap(text)` calls the _same_ `resolveStatRangeFromText` the fortieth item introduced - so "cok export rekap bulan ini"/"cok export rekap september"/"cok export rekap 25 september" all resolve exactly the way the equivalent `"cok rekap ..."`/`"cok siapa yang paling lama live ..."` phrasing already does, with no new range vocabulary to keep in sync across three call sites.

On the storage constraint: the CSV is built as a plain in-memory string (`buildExportCsv`) and wrapped straight into a discord.js `AttachmentBuilder(Buffer.from(csv), { name })` - at no point does it touch `fs`/`CACHE_DIR`, so repeated exports add zero bytes to the bot's own Railway Volume, unlike every other stored feature in this codebase. The format itself (CSV, not JSON or a rendered image) is also the storage-conscious choice for the _user's_ side of the download - smallest reasonable byte count per row - and `EXPORT_MAX_ROWS` (10,000) caps it defensively, though at this bot's actual scale (JKT48's roster, `dailyLog.js`'s 35-day retention) real exports will never come close to that many rows; it exists purely so a pathological future case can't produce an unbounded file, not because it's expected to bind in practice. Values are CSV-escaped per the standard (a comma/quote/newline forces double-quote-wrapping) since a member name could in principle contain a comma, and the file name is derived from the range's own label with everything but letters/digits collapsed to hyphens (`sanitizeExportFileNamePart`) so it stays a valid, readable filename across platforms regardless of what the label's Indonesian text happens to contain.

`chat/router.js` checks for `"export"` + `"rekap"` together _before_ the main `"rekap"` dispatch block runs - "cok export rekap ..." contains the word "rekap" too, so without this ordering it would have been swallowed by the rekap dispatch chain and rendered as a table instead of exported, the same "check the more specific phrase first" pattern used throughout this dispatch chain (§10's twenty-seventh/thirty-sixth items). `replyHelp()` was updated to mention the command, and its "paling lama"/"paling rame ditonton" lines were consolidated into one line each covering the range phrasing the fortieth item just added, rather than leaving the help text describing the old "hari ini"-only behavior - the "cok rekap minggu ini/bulan ini" line's stale "rekap 7/30 hari terakhir" description (left over from before `"bulan ini"` was changed to mean the actual calendar month, §10's thirty-ninth item's sibling change in `chat/router.js`) was corrected in the same pass. Guarded by new `tests/replies.test.js` cases: `buildExportCsv`'s header/rows for a mix of completed and still-live sessions (the latter rendering "Live"/`"-"` rather than crashing on a `null` `durationMs`/`endedAtUnix`), a comma-containing name round-tripping through the CSV-escaping correctly, and `replyExportRecap`'s default-range/explicit-range/empty-range behavior (the empty case returning a plain string with no `files` at all); plus a `tests/router.test.js` dispatch case confirming "cok export rekap" is caught before the general rekap dispatch and produces a file when there's data.

**A forty-second item, the fourth of the five "Q2" features**: "cok bandingin `<A>` vs `<B>`" (`chat/replies.js`'s `replyCompareMembers`) puts two members' stats side by side - total live count, average duration, longest duration, first/last-live dates - with the count leader marked 🏆, plus (an owner-specific ask, "biar kelihatan lebih hidup") each member's profile photo. Both name fragments resolve through `findLiveCountByNameFragment` - the same lookup `"cok berapa kali <nama> live"` already uses - rather than `activeLives`-based lookups, since a comparison needs to work for members who aren't currently live (the overwhelmingly common case), and `live-count.json` is the one store that always has an entry for anyone ever tracked, live or not.

The photo came from a genuinely new capability: `idnApi.js` only ever called `getLivestreams` before, which returns a live _stream's_ thumbnail, not a member's profile picture, and doesn't return anything at all for a member who isn't currently live. Manual GraphQL introspection against IDN's public API (`__type(name: "PublicProfile")`, then `__type(name: "Query")` to find the entry point once the type name was known - introspection on the root `Query`/named types works even though the schema doesn't expose a browsable schema explicitly) turned up `getPublicProfileByUsername(username)`, an existing, independent, unauthenticated query returning `{ username, name, avatar, bio_description, ... }` regardless of live status - confirmed against a real account (`jkt48_nala`) before writing any code against it. `fetchPublicProfileByUsername` (`idnApi.js`) wraps it with the same error-handling shape (`ok`/`errors` checks, throw on failure) `fetchAllLivestreams` already uses.

The avatar fetch is treated as strictly best-effort, mirroring how `replyTodayRecapSoFar`'s external-archive lookup is already treated: `fetchAvatarSafely` catches anything `fetchPublicProfileByUsername` throws (network error, IDN API hiccup, a since-deleted account) and returns `null` rather than letting the whole comparison fail over a missing photo - the two embeds just render without a `thumbnail` in that case, never blocking the text stats. Both members' avatars are fetched concurrently (`Promise.all`), matching the concurrency reasoning already established for the dedicated-channel dual-send in §6. Discord gives an embed exactly one image slot (`thumbnail`/`image`), not two, so rather than trying to composite both photos into a single graphic, the reply carries **two** embeds in one message - `content` holds the "⚔️ A vs B" header, each embed holds one member's own photo and stat fields, and Discord renders multiple embeds stacked in the same message. `chat/router.js`'s regex accepts "vs"/"lawan"/"sama"/"dan"/"dengan" as the separator between the two fragments, covering how this would plausibly be typed in casual Indonesian. Guarded by new `tests/idnApi.test.js` (a new file - `fetchPublicProfileByUsername`'s success/not-found/HTTP-error/GraphQL-error shapes, `global.fetch` mocked) and new `tests/replies.test.js` cases: a full two-embed comparison with mocked avatar URLs and the count-leader crown, the avatar fetch throwing but the comparison still returning both embeds without a `thumbnail`, one fragment not resolving, and comparing a member against themselves.

**A forty-third item, the fifth and last of the "Q2" features, with an explicit owner caution**: "when I asked for a heads-up feature, the owner specifically flagged that JKT48's actual live times are very random, so this needs to be built carefully" - a direct instruction to bias toward silence over a confident-sounding guess that turns out wrong. The feature itself is the mirror image of the existing ending-soon heuristic: instead of guessing when an in-progress live is about to end, `notify/priorityDm.js`'s new `maybeSendHeadsUpAlerts()` guesses when a priority member who _isn't_ live yet is about to start, using the exact same underlying data `"cok jadwal <nama>"` already exposes to users on request - `live-duration-history.json`'s per-member rolling window of the 10 most recent live start times, bucketed into time-of-day (pagi/siang/sore/malam).

The bucket-picking math itself (`computeSchedulePattern`) was extracted out of `chat/replies.js`'s `replySchedulePattern` into a new standalone module, `src/schedulePattern.js` - not because the math changed, but because it's now needed by two call sites, one in `chat/` and one in `notify/`, and this codebase has a strict, previously-unbroken layering where `notify/` never depends on `chat/` (or vice versa; both sit on top of `storage/`/`priority/`/`utils.js` independently). Putting the shared function in either directory would have inverted that layering for the first time, so it went into a new leaf module instead, the same reasoning already applied once to `chat/interactionHelpers.js` (§10's thirty-fifth item). `replySchedulePattern` itself now just formats whatever `computeSchedulePattern` returns - `null` when there are fewer than 3 tracked lives (unchanged threshold, unchanged message), otherwise the exact same output text as before, so no existing test's expectations needed to change.

The heads-up alert's own gate is deliberately **more conservative** than "cok jadwal"'s display threshold, precisely because of the owner's caution above - a user _asking_ "cok jadwal Nala" and getting an iffy guess is low-stakes (they asked, they can shrug off a wrong answer); the bot _volunteering_ a DM unprompted needs to be right often enough to be worth sending at all:

- **at least 5 tracked lives**, not "cok jadwal"'s minimum of 3 - more history before the bot is willing to act on a pattern unprompted.
- **the single most common time-bucket must cover at least 50% of tracked lives** (`HEADS_UP_MIN_ENTRIES`/`HEADS_UP_MIN_DOMINANCE`, `notify/priorityDm.js`) - a genuinely scattered history (lives spread evenly across pagi/siang/sore/malam, exactly the "very random" pattern the owner warned about) fails this outright and never alerts at all, rather than picking whichever bucket happens to have one more entry than the others and presenting that as a real pattern.
- the current WIB hour must actually fall inside that bucket's observed range (`isHourInRange`, handling the same midnight-wraparound case `computeSchedulePattern`'s "malam" bucket already does).
- the member must not already be live (`activeLives.has(username)` - a heads-up for something already happening is pointless, and they already got the real "mulai live" notification).
- **once per member per WIB calendar day** (`storage/headsUpAlerts.js`, a new single-purpose store: `{ username: "YYYY-MM-DD" last alerted }`) - without this, a multi-hour matching window would otherwise re-fire on every ~20-second poll cycle for as long as the window stayed open, which is exactly the kind of spam a "pelan-pelan" ("go slowly/carefully") feature must not produce.

Like the ending-soon heuristic and Party Mode before it, this is framed as a personal DM to the owner (`PRIORITY_PING_USER_ID`), never posted to the shared channel, and every message is explicit that it's a guess (`"👀 Kemungkinan ... bakal live sebentar lagi"`, footer `"Heuristik pola jam live, bisa meleset"`) - consistent with how `sendEndingSoonAlert` is worded, and for the same reason: an unhedged "X is about to go live" that then doesn't happen for hours would train the owner to distrust or mute the bot's DMs entirely, defeating the feature. `maybeSendHeadsUpAlerts(now = new Date())` takes the same optional, test-only `now` override already established for `maybeSendWeeklyRecap`/`maybeSendMonthlyRecap` (§10's thirty-ninth item) - production always calls it with no argument, but tests can pin an exact WIB hour/date deterministically without needing to fake the underlying duration-history timestamps too.

Guarded by three new test files: `tests/schedulePattern.test.js` (`computeSchedulePattern`'s bucket/weekday math directly, including the midnight-wraparound case, and `isHourInRange`'s wraparound handling - both fully deterministic, no clock/storage dependency at all), `tests/headsUpAlerts.test.js` (the new store's per-username, per-day dedup semantics), and `tests/priorityDm.test.js` (a new file - this module had no test coverage before, since exercising it means faking a discord.js `Client`; done here by requiring `discordClient.js` fresh and reassigning its exported `getDiscordClient` to a fake `{ users: { fetch } }` before re-requiring `priorityDm.js`, so its already-destructured reference picks up the fake) covering `maybeSendHeadsUpAlerts` end to end: a strong, in-window pattern sending exactly one DM, a second call the same day sending none (dedup), an already-live member being skipped even with a matching pattern, too few tracked lives being skipped, a scattered/non-dominant pattern being skipped, and the current hour falling outside the predicted window being skipped.

**A forty-fourth item, a follow-up to the "Q2" round rather than one of the original five**: the owner tried "cok bandingin" (§10's forty-second item) typed bare - no `"<A> vs <B>"` - and it fell all the way through to the generic 9-option fallback menu, same as any other unrecognized message. That's a real gap: the word "bandingin" unambiguously signals intent, it just hasn't been told _who_ yet, and getting dumped into a menu with 8 unrelated options is a worse experience than being asked "who?" directly. `chat/router.js`'s existing `compareMatch` regex requires the separator ("vs"/"lawan"/"sama"/"dan"/"dengan") to fire at all, so it correctly does _not_ match bare "bandingin" - the fix is a new check immediately after it, `containsWholeWord(text, "bandingin") -> replyStartComparePick()`, that catches everything `compareMatch` didn't.

The new flow lives in `chat/compareFlow.js` and is a 2-step "search, then pick" UI, since - unlike the small, always-under-25 lists `menu.js`'s existing dropdowns pull from (currently-live members, gifter-snapshot members) - the pool of "every member ever tracked" (`live-count.json`, the same source `replyCompareMembers` itself already resolves fragments against) has no fixed size and no static roster file exists anywhere in this codebase to page through instead; a plain 25-option dropdown of "all members" would silently truncate and get worse over time as more members accumulate history. So each step opens a modal (`ModalBuilder`, a short text input) rather than presenting a list upfront, and the modal submission's results become a dropdown _only when actually needed_: zero matches re-prompts with the same search button, exactly one match is accepted directly (no point showing a dropdown with one option), and more than one becomes a `StringSelectMenuBuilder` (capped at 25 with a note if truncated, same defensive cap `menu.js`'s existing dropdowns use). `storage/liveCount.js` gained `searchLiveCountByNameFragment(fragment, excludeUsername)` for this - deliberately distinct from the pre-existing `findLiveCountByNameFragment` (singular, first-match-wins, used by the typed `"<A> vs <B>"` form where the text already names one specific person) since the search step needs _every_ candidate so the user can disambiguate themselves, not whichever entry happened to iterate first.

Member A's username travels through the flow entirely via `customId` (e.g. `compare_pick:searchB:jkt48_nala`, `compare_modal:B:jkt48_nala`, `compare_select:B:jkt48_nala`) rather than through a new entry in `chat/pendingState.js` - deliberately stateless, so there's nothing to TTL-expire or clean up on close, and the whole flow stays scoped to the one message it lives on (matching the owner's explicit ask below). `searchLiveCountByNameFragment`'s `excludeUsername` param feeds directly off this - member B's search never returns member A as a candidate at all, which also means the flow can never reach the "can't compare someone against themselves" case `replyCompareMembers` (the typed form) still has to check for after the fact. Once member B resolves, the result is built by a new `replyCompareMembersByUsername(usernameA, usernameB)` (`chat/replies.js`) - extracted to share `buildCompareReply` with `replyCompareMembers` so the embed-building logic (avatars, averages, records, the 🏆 crown) isn't duplicated, but skipping the "not found"/"same member" validation the fragment-based entry point still needs, since a username reaching this function was already resolved through the search UI and is guaranteed valid and distinct.

Two explicit owner asks, both about not cluttering the channel: every step **edits the same message in place** (`interaction.update()`, never a new `reply()`) - the same in-place-editing convention already established for `recap_nav`/`fallback_menu`'s multi-step flows - so picking through several search attempts never leaves a trail of intermediate messages behind; and both the search steps and the final comparison result carry their own "Tutup" button (`compare_pick:close`, reusing the one shared `deleteInteractionMessage` from `chat/interactionHelpers.js`, §10's thirty-fifth item), so a wrong click or a finished comparison can be dismissed outright rather than sitting in the channel indefinitely.

Guarded by a new `tests/compareFlow.test.js` (the full step-by-step flow via lightweight fake `interaction` objects, same pattern as `tests/menu.test.js`: the entry reply's content/buttons, "close" calling `deleteInteractionMessage`, both search buttons opening the right modal with the right `customId`, modal submission's zero/one/many-match branches, member B's search excluding member A, and the dropdown-select path for both steps converging on the same result as the 1-match auto-pick path) plus new cases in `tests/liveCount.test.js` (`searchLiveCountByNameFragment`'s multi-match/exclude/empty behavior) and `tests/router.test.js` (bare "bandingin" dispatching to `replyStartComparePick()` rather than the generic fallback menu).

**A forty-fifth item, a direct follow-up to the forty-fourth**: trying the new "cok bandingin" flow, the owner retyped the bare keyword several times while figuring out who to compare. Every attempt is a genuinely new incoming message, so the channel filled up with the same keyword - and, since `chat/router.js`'s `message.reply()` (its one choke point for typed replies) always creates a brand-new bot message, with several identical "cari member A" prompts too. The general shape isn't specific to "bandingin" - _any_ command retyped verbatim piles up the same way - so the fix is a small standalone module, `chat/repeatedReplyGuard.js`, wired into that one call site rather than into `compareFlow.js`. (See the forty-sixth item: the first version of this only deleted the bot's own previous reply on the very first repeat, and never touched the user's typed messages - which is what the owner actually meant by "the same keyword five times in the chat".)

**A forty-sixth item, a round of owner-reported bugs in the forty-second/forty-fourth/forty-fifth items** - six of them, each traced to a concrete cause:

1. **Repeated keyword messages weren't deleted.** The forty-fifth item's first version only tracked/deleted the _bot's_ previous reply, on the _first_ repeat. The owner's complaint was about their _own_ repeated typed messages piling up, and expected deletion to kick in once a keyword was typed _more than twice_. `repeatedReplyGuard.js` now tracks a per-`"channelId:authorId"` streak of _identical_ normalized text (lowercased/trimmed, matching `buildChatReply`'s own `text`): the 1st and 2nd sends are left alone (`REPEAT_KEEP_LIMIT = 2`), and from the 3rd on, _every_ earlier typed message **and** bot reply in the streak is deleted, so only the newest pair remains. A different text in between resets the streak, a streak also ends after `REPEAT_WINDOW_MS` (10 minutes) of silence so retyping a keyword tomorrow never deletes yesterday's messages, and the state update (`registerExchange`) is fully synchronous - rapid typing runs several `messageCreate` handlers concurrently, so state must not change between reading and writing it - with the actual Discord deletes (`pruneRepeatedExchange`) done afterwards. The guard only runs when the bot actually replied, so messages that aren't addressed to the bot are never touched. **Deleting someone else's message needs the bot to hold the "Manage Messages" permission in that channel** (the bot's own replies are always deletable); without it Discord answers `50013`, which is swallowed and logged once (per process) with an explanation rather than on every attempt, and an already-deleted message (`10008`) is ignored silently.
2. **"bandingin A dan B" had no "Tutup" button.** Only the dropdown flow (`compareFlow.js`) attached one; `replyCompareMembers`' result never did. The close row now lives in `replies.js`'s `buildCompareReply` (`buildCompareCloseRow()`, exported along with `COMPARE_CLOSE_ID` - defined there rather than in `compareFlow.js` because `compareFlow.js` requires `replies.js`, not the reverse), so _every_ comparison result carries it, and `compareFlow.js` reuses the same builder for its own search/select steps.
3. **A bare "Nala vs Lily" did the wrong thing.** With no "bandingin" keyword it fell through the whole dispatch chain (typically landing on a single-member status reply or the fallback menu). `chat/router.js` now recognizes `"<name> dan <name>"` on its own (right after the keyword forms, before `jadwal`/`kapan`/etc.) - but since "dan" appears in ordinary sentences, the bare form is deliberately strict: exactly one word on each side of "dan", **and** at least one of them must resolve to a member already in `live-count.json`, so an unrelated "cok makan dan tidur" isn't hijacked. It still respects the wake-word gate: in a normal channel it needs "cok", while in the bot channel/a dedicated member channel (where every message is treated as addressed to the bot) it doesn't.
4. **A real JKT48 member who has never been live was reported as "not found".** `live-count.json` only knows members the bot has seen finish a live, so "Kimmy" simply wasn't in it. There is no static member roster anywhere in this codebase, and IDN exposes no member-list query (its `Query` type only has livestream search, recommended streamers and by-username/by-uuid profile lookups - checked via introspection), but its public profile lookup works for _any_ account regardless of live status, and member accounts consistently follow `jkt48_<first name>` (`isJkt48Member`'s own rule). `replies.js`'s new `describeMissingMember(fragment)` therefore asks IDN for `jkt48_<first word>`: a JKT48 profile means "**Kimmy JKT48** belum pernah live semenjak bot ini mulai mantau" (verified against the real API for Kimmy, Oline and others), no profile means "gak nemu member JKT48 bernama ..." (typo, or a member without an IDN account/with a different username), and an IDN outage says honestly that the check couldn't be made instead of guessing either way. This exposed a latent mismatch in the forty-second item: for an unknown username IDN does **not** return `data: null` as that item's text and test assumed - it returns a GraphQL error ("User Not found"). `fetchPublicProfileByUsername` now maps an errors array made _only_ of "not found" messages to `null` (still throwing for any other/mixed error), and gained a 2-second timeout, because it now runs inside Discord interactions that must be answered within ~3 seconds. Both the typed form and the dropdown flow use `describeMissingMember`; when _both_ names are unknown to the bot, both are explained in one reply.
5. **Comparing a member with themselves.** The typed form only rejected it after resolving both names, the bare/"bandingkan" forms weren't matched at all (so an unrelated reply appeared), and the dropdown flow's step B silently dropped member A from the candidates and then reported "gak ketemu" for a member that clearly exists. Now `replyCompareMembers` compares the _normalized_ fragments first (`normalizeMemberFragment`: case, punctuation and a trailing "JKT48" don't matter - "Nala" equals " nala JKT48 ") before touching storage or the network, and still compares resolved usernames afterwards to catch two different spellings of one member; step B of the flow searches without excluding anyone, filters A out afterwards, and if A was the _only_ match says so plainly (`sameMemberMessage`) with the search/Tutup buttons still attached; `finishCompare` also refuses a same-member select as a last guard. `searchLiveCountByNameFragment`'s `excludeUsername` parameter, now unused, was removed.
6. **The separator is "dan", not "vs".** `compareMatch` is now `/\bbanding(?:in|kan)?\s+(.+?)\s+dan\s+(.+)/` - "vs"/"versus"/"lawan"/"sama"/"dengan" are no longer separators (a dangling keyword such as "bandingin nala vs lily" now just starts the dropdown flow), and the keyword also accepts "bandingkan" and "banding" (the owner used "bandingkan" too, which previously matched nothing). The result header changed from "A vs B" to "A dan B" and `replyHelp()` was updated to match.

Guarded by rewritten/extended tests: `tests/repeatedReplyGuard.test.js` (the >2 threshold, chain behavior through 5 sends, streak reset by a different text, per-author/per-channel isolation, the time window at and past its boundary, real `channel.messages.delete` calls, and failure handling incl. the log-once permission case), `tests/compareFlow.test.js` (never-live vs. unknown vs. same-member messages in both steps, close button on the result, the select-menu same-member guard), `tests/replies.test.js` (the close button on typed results, never-live/unknown/IDN-down/both-missing messages, same-name rejection before any network call, `normalizeMemberFragment`), `tests/router.test.js` (all three keywords, "dan" only, the bare form incl. wake-word gating, "vs" no longer accepted, ordinary sentences not hijacked) and `tests/idnApi.test.js` (the real "User Not found" error shape returning `null`, mixed errors still throwing). The whole scenario list was also run end to end against the real IDN API (Nala/Lily comparison with photos, Kimmy and Oline as never-live, an invalid name, same-member inputs).

**A forty-seventh item, another owner-reported gap**: the reply to the recap table's "🔍 Cari member" search (`replies.js`'s `handleRecapSearchModalSubmit`) had no "Tutup" button of its own. It carried "Ya, biarin"/"Enggak, hapus aja" (the question about whether to keep the _original_ recap table, §10's twenty-fourth item), but answering either one edited the message to `components: []` - so after "Ya, biarin" (or whenever the modal's originating message ID was missing, when there was never any button row at all) a finished search result could no longer be dismissed. `buildKeepOrDeleteRecapComponents` now always returns a row that includes a "Tutup" button (alongside Ya/Enggak while the question is pending, alone when there's nothing to ask), and the keep/delete handler branches re-attach that same lone "Tutup" instead of clearing all components. It uses its own `customId`, `recap_nav:closesearch`, rather than the recap table's `recap_nav:close`: both end in the same shared `deleteInteractionMessage` (§10's thirty-fifth item), but `recap_nav:close` also clears `pendingRecapPage`, and closing a _search result_ must not silently kill the typed "y"/"mundur" paging for a recap table the user just chose to keep. Guarded by updated/new `tests/replies.test.js` cases: the Tutup button present in every search-result shape (with and without the originating message ID, found and not found, and after both "Ya, biarin" and "Enggak, hapus aja"), `closesearch` deleting the message via `deferUpdate` + `message.delete` (never `update`/`reply`), and `closesearch` leaving `pendingRecapPage` intact.
